namespace FluentGpu.SourceGen;

/// <summary>
/// SCAFFOLD (portable build-time generators — see design/subsystems/dsl-aot.md): ElementTypeId, modifier extensions,
/// bitmask DiffProps, the scene-writer (homed in the reconciler/leaf), HookDeps (≤4-arity capture + lanes/transition
/// lowering), Theme blobs, and the WGPU#### analyzers. Win32-free (the COM-binding generator is a separate analyzer).
/// </summary>
internal static class SourceGenMarker;
