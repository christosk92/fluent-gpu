# Wavee Home defect register

This is the implementation-side status register for the redesign handoff. “Build/test verified” is intentionally
separate from “manually observed”; the app is run by the user, so visual/input acceptance remains explicit.

| ID | Status | Fix / evidence |
|---|---|---|
| D1 | Fixed; build verified | `HomeCards.cs` imports `FluentGpu.Hooks`; Debug and Release compile gates cover it. |
| D2 | Superseded by D3 | The separate thumbnail/ghost pair has been removed; `HomeCards.HeroBand` now integrates one complete square cover. |
| D3 | Fixed in code; manual geometry acceptance retained | `HomeHeroLayout` is the shared renderer/estimator arithmetic and pins the flattened surface at 345/305/297 DIPs. |
| D4 | Fixed | `HomeCards.QuickPlay` carries the authored hover scale instead of the dead `1f : 1f` ternary. |
| D5 | Fixed; build verified | `HomeModules.Recents` uses `PagedShelf`; `HomeModuleLayout.ShelfCardHeight/ShelfExtent` are shared with estimation. |
| D6 | Resolved by explicit product decision | Prototype skins retain their own physics; only horizontal browse shelves deliberately use `MediaCard.Shelf` (`Recents`, `Podcasts`). |
| D7 | Fixed | Hero and shelf estimates call the same `HomeModuleLayout` height functions used by their renderers. |
| D8 | Fixed; accounting tested | `HomeLandingProjection` preserves the section directory and projects all peer sections; `LiveShapedPayload_AccountsForEveryCardAndEverySectionTitle`. |
| D9 | Fixed; accounting tested | `SpotifyHomeComposer` composes per source section into the lossless ledger; the 185-card/20-title fixture is green. |
| D10 | Fixed; mapping tested | `artistsets`/`descripto` route to `Topic`; Podcast entities route to `PodcastShelf` in `SpotifyHomeComposer`. |
| D11 | Fixed | `StoreLibrarySource` no longer contributes the three library-tail shelves to Home. |
| D12 | Fixed; tested | `MissingBaselineAndRecents_EmitNoEmptyRows` pins zero-height omission when the live response carries neither section. |
| D13 | Fixed; contrast tested | `WaveePalette.Hairline/HairlineHover` are theme-aware and contrast-bounded; `Hairline_IsQuietButLegible_InBothThemes`. |
| D14 | Fixed; mapper/build verified | `IUserTopService.GetTopTracksAsync` reaches `HomeModules.Artists.TrackActions`, which renders the derived “in your top N” badge. |
| D15 | Fixed; build verified | Artist disclosure content is owned by `Skel.Region` with `StaggerRows` and the shared Home skeleton group. |
| D16 | Fixed; build verified | `HomeModules.EpisodesAndBooks` retains a missing-side survivor inside the authored half-width `TwoColumn`. |
| D17 | Fixed in code; manual input/motion acceptance retained | `HomeArtistRow` now owns `UseMeasuredWidth(...).Value` and renders its selected-state subtree directly, removing the constructor-frozen `Responsive.Of` boundary. Existing pod hover/selection and page/card motion recipes now receive the state change. |
| D18 | Fixed; focused tests green; live-server acceptance retained | `HomeDaylistHydrator` rejects `daylist_pretitle` as an exact title, coalesces URI hydration, invalidates/requeries one exact Pathfinder Home key **at most once per resolved (uri, title) identity — never once per read** (the hydrator is now held for the cache's lifetime), and overlays authoritative headers into groups + sections. Covered by `HomeComposerDaylistHydrationTests` (incl. `RepeatedReads_RequeryHomeOnce_…`) and `PathfinderResourceTests.ExactInvalidation_RefetchesOnce_AndMakesTheFreshBodyResident`. |
| D19 | Fixed; build verified; manual visual acceptance retained | Shell chrome colour was a property of the user's DESKTOP, and the title-bar-vs-toolbar band was structural, not a tuning miss: an unpainted tab rail showed bare Mica Alt while the toolbar carried a translucent `#B3FFFFFF` plate over it — two different materials meeting at a seam. Replaced by a deterministic stack: one full-bleed opaque `Tok.FillSolidBase` GROUND under ALL chrome (title bar, toolbar, sidebar, player dock are now paint-site OMISSIONS, not plates), the page-published material above it, then `WaveeColors.ContentSurface` as exactly ONE opaque step for the content pane and right rail. DWM Mica survives only beneath the login screen and the host fallback, which the authenticated shell fully covers; the "Use base Mica" settings row was removed accordingly (`WaveeSettings.WindowMaterialBaseMica` stays as the startup seed for those two surfaces). |
| D20 | Resolved by explicit product decision | `HomeWashSource` resolves payload-accent-FIRST (`HomeCardMeta.Accent` → graded `CoverColorPlane` scheme → null leg), which is the INVERSE of `HomePage.GroupAccent`'s plane-first order. Deliberate and per spec, not a drift: the shell wash must be in the content's own colours on a COLD feed, before one image byte lands, whereas a per-group accent can afford to wait for the better (graded) answer because it is already on screen next to its art. Neither path invents a colour — the wash's tier 3 does not exist, and a card with neither source contributes no layer at all. |
| D21 | Resolved by explicit product decision | The Weekly wash leg follows the FEED's first `WeeklyPair` card, which is often release-radar, while the rendered module (`HomeLandingProjection.WeeklyPair`) reorders the pair to lead with discover-weekly. So the wash can be tinted by the card that is second on screen. Deliberate: selection in the wash source is by semantic KIND + ORDINAL only, with no format inspection and no copy read, so a renamed section or a non-English account resolves to the same three cards. Adding a `Meta.Format` probe there would buy a cosmetic match at the cost of that guarantee. |
| D22 | Not a defect; inherited derivation | `WaveePalette.ChromeAccent` returns `Tok.AccentDefault` when the lifted art colour is at or below `NeutralS` — i.e. for genuinely greyscale artwork. This is the canonical chrome derivation every accent-filled surface already shares, not a new invented-colour fallback for the wash; the wash's own "no honest colour" answer is still a NULL leg. A greyscale cover therefore washes in the app accent exactly as its Play button and pills already do. |
| D23 | Resolved by explicit product decision | A wash COLOUR change under an UNCHANGED artwork key snaps rather than cross-fading. `ShellMaterialLayer` keys its layer node on `HomeWashPick.Key` (the size-independent artwork key, falling back to the card uri), so the cross-fade is keyed on artwork IDENTITY: a different card cross-fades, while the same card being re-graded (payload accent → landed plane scheme) re-colours in place. Deliberate — the alternative fades the shell under the user for a colour correction of the artwork they are already looking at. |
| D24 | Fixed; ladder tests re-pinned | The light ground was too close to the content rung (#F3F3F3 vs #F9F9F9 — a +6 step the eye reads as "everything white"). `WaveeColors.ShellGround` now drops the light canvas by a pinned fraction (6/243) so NEUTRAL lands exactly `#EDEDED` — the engine's `MicaRef.LightDefault`, i.e. the tone the Files app's bare-Mica tab rail actually shows — giving the chrome a real two-tone read against `#F9F9F9` content (+12). Dark unchanged (#202020 → #282828). Tinted presets keep an equivalent relative step (fraction, not literal). `FloatingChrome` and `PresetSwatch` follow the ground via the indirection. |
| D25 | Fixed in the engine; gate B.4b + dnd pitfall 15 | Tabs and library cards intermittently ate clicks by POSITION: `DragBit` alone made a `Draggable`-only child the hit target, and click activation/pointer-release fired on that node only — a click black hole inside a clickable ancestor (the tab label lane, all four library card content builders). The input dispatcher now walks to the nearest click-owning ancestor on release (mouse release also matches on the OWNER, so press/release across children of one plate is one click). Library drag sources additionally gained the click-primary ×2 threshold. |
| D26 | Fixed; select-in-place + `LibrarySelectionCommit` tests | Library search hits navigated away to detail pages, contradicting the page's own master-detail contract (song hits already played in place). Artist/album hits now commit the browse selection (clearing the filter, drilling in collapsed mode, bring-into-view via `ItemsViewController`); `SyncDisco` snap-to-first now fires only on an EMPTY album key so a search-committed release is never clobbered. Side find: the `ItemsView` selection round-trip was silently wiping the committed (and persisted) album key — guarded with a re-entrancy flag. |
| D27 | Fixed; `LibrarySearchPerfTests` + parity test | Library search ran a full-corpus rescan with per-entity cold hydration per KEYSTROKE, uncancellable (superseded scans ran to completion holding both store locks) — seconds of latency and "…" columns. Now: 180ms debounce (search path only; browse narrowing stays raw), real cancellation at every loop boundary, thin-first matching (zero point-reads for non-matching entities; survivors hydrate bounded), a per-scope corpus cache invalidated off the `StoreChange` seam (+30s TTL backstop), and `Skel.Region` row shimmers with keep-previous-results instead of "…". Result content and match-reason hierarchy pinned identical by a parity test. |
| D28 | Landed; ladder + band + gates green; manual acceptance pending | The two 48px chrome rows merged into ONE title-bar row: Zune text-first tabs (no plates/flares, layout-anchored FLIP underline, hover-only close, no '+' — Ctrl+T/middle-click/⌄ menu), window-centered omnibar that collapses to a click-expanding icon (180ms, Ctrl+K), and the `MergedChromeLayout` priority ladder (name drops first → search 420→220 → tabs 200→110 → search→icon → LRU tabs into ⌄, active never leaves; hysteretic, 11 tests). The TopBar shortcut band moved render sites into the sidebar (all three designs + rail + drawer, zero schema change — same reducer/undo/customizer). Engine gained the TitleBar merged mode (`CenterContent`/`Trailing`/`ContentVersion`/`ShowRailBaseline`) + the repo's first headless drag-region gate. |
| D29 | Resolved by explicit product decision (hybrid) | The all-opaque deterministic shell (D19/D24) read as "one solid slab" once the merged row landed, and the user chose the HYBRID — the Files model: CHROME is Mica-passthrough (the shell root paints nothing; the merged row means no plates, so the two-material seam D19 fixed cannot recur), while the PAGE (content pane + rail band) stays OPAQUE `ContentSurface` so page legibility and wash colours never depend on the wallpaper and keep the no-blend PSO. `ShellGround` (#EDEDED/#202020) survives as the no-Mica fallback and floating-surface flatten base; page tints (A=0.14/0.05) are once again scrims over live material. |
| D30 | Wave 1 landed (token convergence); gates green | The audit's "undeclared second ramp" is gone: ~250 W1b + ~200 W1a literal conversions onto the engine ramp (sizes, weights 400/600, line heights via aliases), radii onto 4/8/16/Circle, spacing onto the 4-grid, gutters onto `Spacing.PageWide` 36 with a shared `WaveeSize.SectionGap` rhythm and a 1600 `PageMaxW` cap (Home gains it). The fluid Detail title is a clean 28→40 step; the length-driven immersive title is fixed 40 with one documented fallback; MediaCard's 37 hand-rolled on-media colors collapsed into the 13-token `WaveeOnMedia` ladder; `RichText` now pins the 20px line height for 14px text. Deliberate keeps: lyric-line weight 700 (signature type), the 10px "E" badge (documented sub-ramp exception), display-alias 700s. Pinned by `DesignTokenConvergenceTests` + re-derived `HomeHeroLayoutTests` (tier heights now sums of on-ramp blocks). |
| D31 | Wave 2 landed (motion systematization); gates green | Hover/press motion is Wavee's identity and is KEPT — but it was authored as 69 call sites across 20 distinct scale values (1.005→1.16 hover, 0.625→0.997 press) with only ~5 honouring reduced motion. `WaveeMotion` is now the live vocabulary: three interaction tiers (`ScaleSubtle` 1.02/0.98 rows+chips, `ScaleStandard` 1.04/0.96 buttons+pills+tiles — the tier `WaveeCta`'s private 1.04/0.97 constants folded into, `ScaleEmphatic` 1.07/0.92 media FABs+transport+on-artwork) reached ONLY through accessors that read reduced motion, plus the WinUI duration ladder 83/167/250 that ~15 raw literals (100/120/140/150/180/300/333 ms) snapped onto. The reduced-motion read has to be app-side: `AnimScheduler.SeedEased` (the hover/press seeder) is the one animated path carrying no `ReducedMotionPolicy`, and `SceneRecorder` composites the scale unconditionally — so the engine never suppressed a single hover cue, and now the VALUE does. Also: the card contract's hover plate dropped from `Elevation.CardHover` (blur 16 — the same band as `Elevation.Flyout`) to `Elevation.Card`, so a hovered card no longer out-shouts real popups; the −4px lift and plate reveal still carry the state. Deletions: the sub-perceptual 1.005/0.997 tour banner (below the recorder's own cull threshold) and the dead `PageInMs`/`PageOutMs`/`Slow` tokens (no call site); `StaggerMs` survives as a declared Wave-5 forward reference. Deliberate keeps: `DetailTracks`' 10f/16f selection pill (WinUI `ListViewItem` parity, not an interaction tier) and `SeekBar` (derives from `Slider.DefaultStyle`). Pinned by `MotionSystemTests` — tier values, ladder ordering, reduced-motion collapse, and a source scan that fails on ANY new raw `HoverScale`/`PressScale` literal. || D32 | Wave 3 landed (voice unification); gates green | The app said the same thing several ways on several surfaces, and each way had drifted on its own. Five idioms are now ONE each. **Eyebrow**: the ALL-CAPS + letterspaced micro-header (neither Fluent's sentence case nor the editorial register the rest of the app aims at) is gone — 58 sites across 32 files onto `WaveeType.Eyebrow`, sentence case throughout (the caps transform is deleted, not relocated: `.ToUpper()` over a LOCALIZED string mangles Turkish dotted i and expands German ß, and Home's greeting was shouting the user's own display name back at them), with ONE tracking (`EyebrowTracking` 30) replacing NINE hand-picked values (10…120). Accent eyebrows KEEP their accent — concert date captions, artist-shelf eyebrows, the module accent facts are the identity. **CTA**: `HomeCards.HeroButton` — a private 32px/13px cluster whose own comment declared the divergence — is deleted; Home's hero now speaks `WaveeCta` (accent capsule + standard capsule + the new icon-only arm `WaveeCta.Icon`), and WaveeCta gained the icon-button GEOMETRY TABLE: 32×32 r4 standard, 36 circle only inside a CTA cluster, circular FABs only ON MEDIA. QueuePanel's two 28 circles and SearchPage's duplicate FollowButton went to the table; FriendsPanel's hand-rolled RetryPill became `Button.Standard`. **Facets**: DiscographyPage's hand-rolled crumb + hand-drawn chevron + 28px-skinned DropDownButton became stock `BreadcrumbBar` + `PageHero` + `SelectorBar`. Investigated and deliberately NOT converted: Concerts' genre tokens are MULTI-select and Browse's category chips NAVIGATE (Role=Hyperlink, no selection model at all) — a SelectorBar would advertise a state neither can hold; both were restyled quiet instead, and the concert token's insert-a-checkmark-on-select (which slid its own label sideways and reflowed the whole strip) is gone. **Empty state**: one grammar — display-face headline, optional caption, ONE QUIET action (`Button.Standard`, never accent: the accent budget's action rung belongs to the page's real primary), and NO decorative glyph (the `glyph` parameter is removed, so a stale call site is a compile error). Six rogue copies repointed (SearchPage, HistoryPage, FriendsPanel, NowPlayingPanel, QueuePanel, LibraryPage ×4); `ErrorState` now routes through it; ONE variant, `EmptyState.Compact` (Subtitle headline), serves the ≤340-DIP rails. **Lists**: library search rows adopt the browse-list SubtleFill selection instead of an accent-tinted plate; search result rows drop 20 stacked hairline cards for transparent rows + SubtleFill hover; zebra is now DERIVED (`Tok.FillSubtleTertiary` + `ColorContrast.Over` for its interacted rungs — which also fixes dark, where the stripe WAS the hover fill) and confined to the long tracklists (removed from QueuePanel + FriendsPanel); the audiobook rating is the real read-only `RatingControl` instead of `ProgressBar`-as-rating (the old comment rejected it on allocation grounds measured against a workload this bounded shelf does not have). **Accent budget**: `WaveeAccent` documents the three roles (Action / Selection / Decor) and enforces the two hard rules — the artist section header's 3×22 r1.5 capsule (pixel-for-pixel the SELECTION-indicator geometry, eight times down one page) is now a 20×2 rule under the header text, and the dashed accent border + accent chevrons on the concert surfaces went to neutral chrome. Everything else accent-decorative KEEPS its accent. Pinned by `VoiceUnificationTests` — the single-tracking pin, a source scan that fails on any new positive `CharSpacing` or any caps-transformed eyebrow, the empty-state grammar pin, the zebra derivation + quieter-than-hover invariant in both themes, and both accent-budget rules. |
| D33 | Wave 4 landed (structural polish); gates green | Seven structural defects the earlier waves' vocabularies could not reach, because each was a SHAPE rather than a token. **Transport hit targets**: the player bar ramped its secondary buttons 32/30/28/26 and its primary 36/36/34/30 across the six pressure tiers, so the three NARROWEST tiers — exactly where a mis-click costs most — shipped sub-WinUI-minimum targets. The metric table is now FLAT (32 box / 16 glyph everywhere; primary 40/20 from Medium up, 36/18 at Compact/Minimal), pressure is absorbed by what the row DROPS and by the spacing rungs, and the identity block gave back 8 DIP at the two narrow tiers to pay for it; the heart lost its 30/15 cap (it was the one affordance that survives to the 300-DIP floor, below the floor). The 300-DIP row still leaves the SeekBar ~48 DIP, which is pinned. **Toggle grammar**: shuffle/repeat/devices/lyrics/queue said "on" with a 3 x 3 accent DOT under the glyph — smaller than a full stop at 100% scaling, sitting where the glyph's own descenders live, with no hit/focus/high-contrast meaning. It is now the stock `AppBarToggleButton` visual: accent glyph over a `Tok.FillSubtleSecondary` plate, hover one rung above it (Tertiary), 83ms brush cross-fade on the toggle edge, rest still completely unpainted. The file's old "a filled background is reserved for the hover/press axis" rationale is what forced the ornament, and WinUI does not honour it — checked fill and pointer-over fill are different rungs of one ramp, which is how a latched control is told from a hovered one. **Volume thumb**: 12/8/6 against `Slider.DefaultStyle`'s 22/12/10 — a 12-DIP grab target on the control the user drags most; it now simply passes no style (the 4px/2r track WAS already the default; only the thumb was overridden) and its `thickness` clears the 22-DIP ring so the slider's own over-thumb hit test isn't clipped. **Splitter**: the engine has no GridSplitter/PropertySizer (checked), so `ColumnGrip` is the app's — and it was a 7-DIP strip around a PERMANENT hairline whose `HoverFill = Tok.TextTertiary` (a text token as a fill) only lit when the pointer was over the 1-DIP line itself, because a fill-only child is NOT driven by its container's hover (`AnimScheduler.SetHoverDescendants` cascades to reveals only). All four seams (library x3 + the detail rail) are now the stock model: a 16-DIP invisible strip with an opacity-revealed 2-DIP `FillControlStrong` indicator that lights from anywhere in the strip and stays lit through the drag. **"Go to artist"**: the discography pane's ONLY route to the artist page was a grey 16-radius capsule in TextSecondary — capsule + grey + secondary ink is what a DISABLED pill looks like everywhere else in this app. Now the stock HyperlinkButton treatment (AccentTextFill ramp, ControlCornerRadius 4 so it cannot be read as a CTA, SubtleFill hover/pressed); the arrow glyph stays and takes the link ink. **Rail buttons**: `RightRail`'s header buttons (32 box / 12 glyph) and `ImmersiveLyricsSurface`'s twin (36 box / 14 glyph) were the same control drawn two ways — a bigger box wearing a smaller glyph than its sibling. Both are now row 1 of WaveeCta's icon table (32 x 32 r4, 16 glyph) off one shared constant. **Nested-axis scrolling**: the album page's trailing column stacked four horizontal `PagedShelf`es inside its own vertical scroller, so the user had to switch gesture axis per section AND the item COUNT a section showed depended on an unrelated splitter position. More by / Similar / Featured are now capped vertical stacks of `MediaCard.Row` (48 cover, hover play FAB, drag source), merch a bespoke `MerchRow` (no play affordance; its second line is a PRICE, so it sits trailing in link ink), each with a quiet `HyperlinkButton` "Show all" that expands IN PLACE — the rows are already loaded, and a link that navigated to a fifth surface to show four more albums would be worse than the shelf it replaced. The trailing skeleton now reserves ROWS instead of a 150-DIP card strip. **Shelf chrome**: `Rail`'s pager was two permanently-filled 32-DIP circles (circles are the geometry table's FAB-on-media row, not chrome on a flat page); it is now a borderless 32 r4 square at 0.7 rest opacity that comes to full strength on the HEADER ROW's hover. The hover scope is the header, not the shelf, and that is forced: the engine reveals descendants from any interactive ancestor and recurses through non-interactive wrappers, so scoping on the rail root / viewport / card strip would hand every `MediaCard` its container's hover and pop ALL of them at once — and no wrapper fixes it, because a hover boundary is by definition interactive and cascades from itself. `Surfaces.SectionBand` dropped its card fill + hairline for an unbordered wash (the shell's published material already carries page tint); it is layout-neutral and currently has no live call site, kept as the one recipe so the treatment cannot be re-invented with a stroke. **Section ornament**: Wave 3's 20 x 2 rule sat 8 DIP under a 28-DIP line box (a 2-DIP column gap plus a 6-DIP margin), which read as a separate object under the header and grew every `PagedShelf` header row ~10 DIP past the 32-DIP chevrons beside it; the margin is now 2, so the total is 4 and the shelf header grows +2. Pinned by the transport floor table + flat-ladder + 300-DIP budget theories in `PlayerBarResponsiveLayoutTests` and the new `SplitterGeometryTests` (strip width, all four wrappers routing through `ColumnGrip.StripW`, and the indicator being an opacity reveal on a neutral grab token rather than an accent fill). |

| D34 | Wave 5 landed (the Zune layer); gates green | The last expressive pass, now that Waves 1–4 have made everything read quiet. Three things. **Entrance choreography**: `WaveeMotion.StaggerMs` shipped in Wave 2 as a declared forward reference with ZERO call sites — a token nobody reads is a comment, not a decision — so this wave either wired it or deleted it. It is wired through exactly one recipe, `WaveeEntrance` (rise 8 DIP + fade + 2px blur, `Expressive.Slow` tween, `Channels = Opacity` so it can never arm a layout FLIP on the node it decorates), with the two properties the ad-hoc spelling lacked: a **cap** (`StaggerCap` 8 — items past it share the cap's delay, so the whole cascade is bounded at 360ms however long the list is; `Element.Stagger`, the engine's declarative parent-side spelling, is `index × ms` with no ceiling, which is a two-second arrival on a 50-row list whose tail animates off-screen), and **reduced-motion-as-a-value** — `DelayMs` reads the flag and returns 0, the SHAPE is identical in both modes, and the engine's own `ReducedSnap` parks the rise and blur while keeping the opacity cross-fade. That last part is not stylistic: the previous stagger attempt gated an entrance HOOK on `Motion.ReducedMotion`, which changes the hook COUNT between renders and crashes the reconciler when the flag flips mid-session — and a resize grip flips it (`ColumnGrip.OnDown` carries the note). **Where it went, and where it deliberately did not.** A surface qualifies only if its items mount ONCE: an entrance replayed as a row scrolls back into view reads as flicker, not choreography. So it is on `SearchSongs` (the bound Songs list — the engine's `RowBind` recycler REBINDS a slot rather than re-mounting it, so `scope.Index.Peek()` at realize is the item's initial position and a scroll never replays), where it REPLACED that file's private copy of the whole idea (its own transition constant, its own cap of 12, its own `!Motion.ReducedMotion` gate, its own read of the engine's twin rung `Expressive.Stagger`); and on `BrowseDirectory` — Search's landing surface, eager and fixed-length — whose title and taxonomy bands now cascade in 40ms apart (its `Skel.Region` dropped to `SkelReveal.None` so the content owns its entrance instead of a block blur-reveal fading in bands that are still arriving). It is NOT on Home's module shelves and NOT on the library master list, and that is a finding rather than a shortfall: Home is a `Virtual.Measured` row list on the `RenderItem` path (the page's own `HomeShowAll` comment documents that a row's state is re-created on every realization — i.e. rows genuinely unmount and re-mount on scroll), `HomeSectionPage` is a `Virtual.Grid`, and `LibraryPage.ListBody` is `ItemsView.Create/List`; all three would replay their entrance mid-scroll. They are not un-choreographed either — the engine already gives their INITIAL population a stagger through `Skel.Region(reveal: SkelReveal.StaggerRows)`, fired once on the shimmer→real swap and never again, at `Expressive.Stagger` = the identical 40ms. The fence is a test, not a comment. **Tab strip de-selection**: the Zune text strip expressed "not selected" as `Opacity = 0.6` on the tab plate (0.85 on hover) *on top of* a foreground that was already `TextSecondary` — the two compound, so an inactive label rendered at 0.6 × secondary, below any dimmed-text rung the theme defines; it dimmed the tab's icon and its close glyph by the same factor even though neither expresses selection; and a high-contrast or custom palette can retune `TextSecondary` but cannot retune a hard-coded 0.6. It is now purely the token ladder — `TextSecondary` at rest, `TextPrimary` when selected (with the 650 weight and the sliding accent underline still carrying selection) and `TextPrimary` on hover via `TextEl.HoverColor`, which eases with the plate's own `HoverT` (`SceneRecorder.ResolveTextColorCore`), so the ramp is still the engine's fade and still not a hovered-index branch. Plates sit at full strength. **Big-type page titles**: `LibraryPage` had no title at all — the kind was legible only from the sidebar selection — so `WaveeType.PageHero` now opens the master column INSIDE its toolbar rather than as a band above the columns, because the library is a master–detail browser whose right-hand panes belong to the selection and a full-width title would be titling three surfaces at once (suppressed in the collapsed layout, where `CollapsedCrumbBar` already names the kind and is also the drill-out affordance). Search's landing title moved from the raw `Ui.Title` factory onto the `WaveeType.PageHero` alias. History was left alone: it already opens on `PageHero` at the `Spacing.PageWide` gutter, and a second title is the double-titling this converges away from. Pinned by `EntranceStaggerTests` (the delay ladder IS one `StaggerMs` per step, the cap, the negative-index clamp, the terminal's shape and channel mask, the reduced-motion collapse to an identical shape, a live-call-site scan, a scan that fails on any app-side read of `Expressive.Stagger`, and a by-name fence keeping the recipe off the four virtualized surfaces) and by the new VerticalSlice gate `B.9c-text TabStrip Text selection is a TEXT-TOKEN ramp` (997 checks). |
| D37 | Fixed in code; gates green; manual acceptance retained | **The icon-mode search click-expanded to 40 DIP.** The merged chrome row is allocated by a pure space ladder (`MergedChromeLayout.Resolve`) that resolves `SearchMode = Icon` under pressure; `MergedSearchIsland` then click-expands via its `_searchExpanded` latch. But the ladder never learned about the latch, so the "expanded" field was sized DOWNSTREAM — `FieldWidth()` clamped `ChromeSearchExpandedW` against the bar's measured centre column, and that column is ~the icon's own 32 DIP while the row is still collapsed (it is one of THREE equal-weight grow siblings, so it measures the island plus a third of the slack). Result: a 40-DIP field with a 40-DIP suggestion dropdown under it. **The expansion is now an INPUT to the ladder** (`Resolve(width, tabCount, previous, searchExpanded)`): the field claims its width IN PLACE and the row FOLDS to fund it, in the ladder's own priority order — profile name (90) -> bare avatar, Friends (44) -> profile-menu row, then tabs (110 each, less the one-off 36-DIP "v" plate) into the overflow, `KeepTabs >= 1` always. Forward is deliberately NOT on that list: `FixedBudget` makes its rung cost-neutral (the button it removes and the "..." it forces in are the same plate), so folding it would cost an affordance and free nothing. Each rung is taken only while the row is still short of the hard floor `ChromeSearchExpandedMinW` = 240 — a squeeze the name alone pays for leaves the Friends button and the whole strip standing — and the field then grows into whatever the folds returned, capped at `ChromeSearchExpandedW` = 380. Folding on toward 380 was rejected: an icon-mode row has under `ChromeSearchMinW` of spare BY DEFINITION, so a 380 fold target folds every identity bit at every reachable width, i.e. a ladder with one rung. **Hysteresis:** entry is user-initiated so it is applied AFTER the hold and never gated by it (the latch opens an icon the promotion reserve is still holding); release is the ordinary machinery. **Infimum poisoning:** the expansion's folds must never be recorded as HELD demotions, or the promotion reserve would keep the row lean long after the latch dropped — so the shell keeps the UNEXPANDED ladder beside the published one (`WaveeShell._chromeBase`, a plain field, written only from the off-render ladder effect) and always resolves from THAT; `MergedChromeLayout.SearchExpanded` marks the results that must never be fed back. `FreeSpace` itself is a pure function of width and is untouched: the expansion spends the DIP its folds return (`reclaimed`, threaded into `Compose`) rather than re-deriving a looser base, which keeps the allocation conservative and `FootprintFor <= width` exact. **ContentVersion** bumps in BOTH directions and twice over — bit 16 is the latch, bit 32 the flag, and flags/SearchWidth/KeepTabs/hidden-count all move with them now that the latch is a ladder input — so the non-client region report can never describe the old rects. The "v" badge count re-resolves through the existing `KeepTabs` projection effect. `FieldWidth()` now returns `l.SearchWidth` outright whenever the ladder resolved a field; the measured-column clamp survives on the ICON branch only (the one frame before the ladder re-resolves, and the widths below `MinimumExpandedWidthFor` — **764 DIP with one tab open, 800 with more** — where the row genuinely cannot fund a 240 field beside even one tab and keeps the magnifier). That floor sits ABOVE the app's 300-DIP minimum window, and is stated rather than dressed up: lowering it means letting the expansion count the tab strip below its 110 floor, which is a layout decision (the strip clips; the caption cluster must not move) and is not taken here. Pinned by 33 new `MergedChromeLayoutTests` cases — the 40-DIP regression at the reported widths, a real field at every collapsed width, the funding order as a per-width invariant PLUS a proof that all three rungs are actually reached, reserve-independence on entry, an exact latch-drop round trip, a latch-flapping resize sweep whose carrier stays bit-identical to the untouched one, the pinned minimum widths, and space honesty across the expanded sweep. |

| D35 | Wave 6 landed (the text-chrome sticky header); gates green | The app had TWO unrelated sticky page headers, and neither was a header. **The artist page** collapsed its hero into an art-tinted 56-DIP band carrying a 36-DIP circular avatar, the artist's name and the Play / Following capsules — a smaller redrawing of the hero that had just scrolled away, which is the one thing a collapsed header must not be: it repeated what the visitor had already read and spent the page's whole sticky budget doing it, while the thing they genuinely could no longer do (see where they were in a twelve-section magazine, or jump) had no affordance at all. It also carried a tier-driven policy that DROPPED Follow at narrow widths, because two capsules plus an avatar plus a name do not fit a phone-width row. **The track-detail pages** had no band: they had three FLOATING objects over live scrolling rows — a shadowed, bordered, accent-tinted capsule holding a cover + title + owner, a bare 32-DIP search glyph, and a 40-DIP accent circle play FAB — with nothing behind them, so rows slid under three unrelated shapes, and an 8-DIP scroll-ramped `Elevation.Card` strip under the column header was compensating for a translucent bar content ghosted through. The filter funnel existed ONLY as an affix inside the EXPANDED search field, so it was unreachable from the collapsed bar entirely. Both are replaced by ONE recipe: the **text-chrome context band** (Concept C, Zune-pivot lineage) — `ContextBand` + the engine-free `ContextBandLayout`. **The material**: `WaveeColors.ContextBand`, an OPAQUE flatten of the content layer (`ColorContrast.Flatten(FileArea, MicaRef.*Default)` — the same idiom `PresetSwatch` uses, so the band cannot drift from the region it abuts), one 1-DIP separator hairline, and NO shadow — an opaque surface is already a boundary and Zune chrome carries no elevation. No thumbnail, no plates: the chrome is typography. **The artist band** is title · pivot · actions, where the pivot is the page's OWN sections as text links (Top tracks, Albums, Singles & EPs, … Fans also like — the page's existing localized strings, built in the same pass as the section list so the two cannot disagree) with a 2-DIP accent underline on the one currently under the band. That underline is AccentSelection doing its actual job, the same family as the tab strip's, and with the primary Play it is the band's only accent. **The detail band** is title + owner·count byline, and Find / Filter / Play as text actions on the same handlers the three deleted objects carried; the tracklist's column header pins directly beneath it and the two read as one surface with the column row's own rule as the band's single hairline. **The scroll spy** is the `ShyMonthPill` seam, not a new one: sections register their wrapper node in a page-owned `SectionAnchors` registry, the band's pivot component subscribes to the page's ALREADY-published scroll signal in an EFFECT (never in Render) and writes the active index through `SetIfChanged`, so a full-page scroll costs a handful of re-renders of one small cluster — the magazine body, the virtualized discography grids and the band's own title and actions are never touched; a click routes through the engine's one `ScrollIntoView.BringInto` seam against the page's explicit viewport. **The new grammar and its fence**: `WaveeCta.TextAction` is a third CTA grammar (plateless 14/600, `TextSecondary`→`TextPrimary` on the engine's own HoverT, accent ink on the stock hyperlink ramp for the ONE primary and for a latched toggle, `AutomationRole.Button`, focusable, hand cursor, sentence case, and deliberately NO hover scale — a word that grows inside a 56-DIP bar shoves its neighbours), documented as scoped to context bands and pinned there by a call-site scan. **Priority under pressure**: title and actions never drop; the PIVOT yields, from the right, so later sections go first and the survivors are always a prefix of the page order — there is no overflow chevron, because a menu holding three section names is not wayfinding and everything it would hide is reachable by scrolling, which is the gesture the pivot shortcuts. **Deleted, not hidden**: the tinted bar's avatar/acrylic-fallback recipe, `ArtistCompactBarPolicy`/`CompactBarPolicyFor` and its test, the floating capsule + its `CompactPillWidthCap`/`CompactArtworkSize`/`CompactPlaySize`/`CompactPillMaxWidth` geometry ladder (only the 44-DIP reveal-band TIMING constant survives, renamed `CompactRevealBand`), and the chrome shadow strip. Pinned by `ContextBandLayoutTests` — an exhaustive width ladder for the fit (prefix truncation, the never-loses-an-item-while-widening monotonicity walk, degenerate and below-floor widths), a top-to-bottom scroll walk for the spy (monotone advance, arrival measured against the BAND's edge rather than the window's, NaN-stops-the-scan for unrealized sections), plus source gates for the opaque flatten, the no-elevation rule, the TextAction grammar and its fence, and the by-name absence of every deleted style. |

| D38 | Wave 7 landed (the unified detail hero + the page tone); gates green | **The hero was three heroes.** `DetailVerticalHero` selected between them with a SECOND breakpoint ladder (a hero-orientation enum with its own hysteresis bands at 560–600 and 400–440) layered under the page's own mode ladder, and the three arms shared almost nothing. The IMMERSIVE arm was a full-bleed square cover with a bottom `EdgeFade`, a four-stop black scrim over it, on-media white ink throughout, a raw Display run at −28/−16 tracking whose SIZE came from the title's LENGTH (40 or 28 at 40 characters), a 17/400 attribution, a white 40×132 Play pill, hand-rolled white-alpha “glass” circles, a floating 60-DIP utility strip, a 44-DIP art token in the lower edge and copy indented to x=80. The COMPACT arm was a 96/64-DIP thumbnail row on ordinary surface ink with an accent 36×92 Play and no description at all. The SIDE-BY-SIDE arm was **dead code in auto mode** — it needed ≥560 DIP of track column inside a layout that only exists below 540, so it was reachable only through the `Hero` page-layout setting. A resize therefore did not adjust the hero, it swapped it for a different design; and the widest thing most users could reach was the one nobody had looked at. **Now there is ONE composition** — artwork · eyebrow · title · 20×2 accent rule · attribution · meta · action row · description, left-aligned at every width — and width decides only SIZE plus one flow axis: stacked below 540, artwork beside the identity column at/above it (24-DIP hysteresis), which is a REFLOW of the same children in the same order, not a variant. Every part is a component the app already shipped: `DetailRail.EyebrowRun` / `HeroArtwork` (the rail's card corners, card elevation and 1.18 oversaturation), `Surfaces.AccentRule` (the same section ornament the artist page's eight headers carry), `WaveeCta.Play` on the artwork accent, and 32×32 `Radii.Control` satellites on `WaveeCta.IconButtonSize` — row 1 of the icon-button geometry table — for shuffle / save / share / more. The title is STEPPED (≥640 → 40/52, ≥360 → 28/36, else 20/28), never fluid and never length-driven, with the ramp's own tracking. `WaveeEntrance` cascades the identity children on mount; the artwork does not stagger, because it is the anchor. **THE PAGE TONE.** The pages stopped stacking alpha washes on a neutral ground and now HAVE a ground: `WaveePalette.PageTone` — the cover's HUE at a page lightness that is FORCED per theme (dark L=0.15 · light L=0.89) with saturation CAPPED (0.30 / 0.42), and the neutral `#151515` / warm `#F6F4F1` answer for art with no hue worth using. Two rules, both load-bearing: complementary-not-sampled (the page is the record's colour without being a re-print of it) and the CLAMP (Apple's unclamped version of this is its loudest complaint; the clamp is also what guarantees ink polarity by construction, which is why the on-media ladder could be deleted from these pages outright rather than re-tuned). It is ONE opaque plane at the shell root behind BOTH arms — `CoverPaletteLeaves.PageTonePlane`, a cover-keyed leaf with the `Watch` inside it, a bound `Fill` and a 250 ms brush ramp, so a grading arrival cross-fades one node and never re-renders a page. **The background extension** (art melting into the surface) rides the same node as a σ-72 `ImageEl.BakedBlur` copy at 0.5 resolution, saturation 1.35, masked to zero at the hero's lower edge — the blur is baked ONCE into a derived texture and is then an ordinary quad, so scrolling costs no Gaussian (a `BoxEl.Blur` would have been a pooled offscreen RT plus a ≈3σ-halo separable pass every frame). The sticky context band follows the ground: `WaveeColors.ContextBandOver(tone)` — the same flatten, with the surface as an argument — bound through a shell-owned signal the plane publishes, so the band cannot read as a grey plate on a tinted page. A new Settings ▸ Appearance row limits the tone to the hero band; a missing grading and `DisableColorWashes` independently mean NO tone at all (the band then falls back to the constant flatten). **Deleted, not hidden**: the orientation enum + both hysteresis ladders, `SideArtworkSize` / `MinimalHero*` / `Compact*Hero*` / `ImmersiveIdentityTokenSize`, the immersive scrim / utility strip / token layer / artwork `EdgeFade` / x=80 indent, the immersive save button, the glass helper and its fill table, the on-media arms of the title / attribution / description (including `PlaylistInlineEdit`'s whole `onMedia` axis, whose slot now carries a `displayFace` flag so an editable playlist's hero title and an album's sit on the same FACE as well as the same rung), the title-length sizing, the `_verticalHeroImmersive` echo signal (its slot now carries the hero's MEASURED HEIGHT up to the tone plane), both detail-wash mounts, `CoverKeyedWash` and `Surfaces.DetailHeroWash`. Pinned by a rewritten `DetailVerticalLayoutTests` (a 240→1400 width LADDER: one flip per sweep, monotone artwork, whole-DIP edges, the hero floor that never becomes a row thumbnail, row-flow geometry that fits its column, exactly three title rungs, plus by-name absence scans for every deleted token) and a new `DetailPageToneTests` (the clamp box in both themes, hue survival, complementary-not-sampled, greyscale → neutral, null → null, and WCAG 4.5:1 polarity for standard ink on every tone the ladder can produce). |
| D39 | Wave 7b landed (the context band's OFFSET model); gates green | **The band read as a solid black slab** (user report, screenshots). Root cause, and it was never a tuning miss: the band painted an opaque FILL, and every fill it ever wore was an APPROXIMATION of a surface it could not observe. The last one was the honest version — `ColorContrast.Flatten(FileArea, MicaRef.*Default)`, the translucent content rung composited onto a no-wallpaper reference tone, chosen precisely so an opaque band would not seam against the region it abuts — and it still could not follow LIVE MICA, which takes its colour from the user's desktop. On a dark wallpaper the reference tone landed as a black plate across the page. D38's fix (parameterising the flatten over the detail pages' new art tone, `ContextBandOver(tone)`) repaired one of the two consumers and left the artist page exactly as wrong. **The band now paints NOTHING.** Scrolled content is CLIPPED at the band's lower edge instead of sliding under it, so the band's DIP are an unpainted OMISSION and what shows there is the page's real ground: the shell's `FileArea` smoke over live Mica on the artist page, the art-derived `PageTone` plane plus its blurred backdrop on the track-detail pages — which means the detail band inherits the record's own colour for free, with no plumbing, and there is no constant left to drift. **The clip is now the contract**, and the engine already had both mechanisms with this exact intent in their docs (`ApplyStickyClip`: "content scrolling under chrome pinned on that line is guillotined there, so the page's real backdrop shows behind the chrome"). The two detail paths ALREADY clipped at the full band height and needed no change — the trailing (album/show) scroller's `ClipTopAtViewport` and the virtual (playlist/liked) list's `ItemClipTopInset`, both at `DetailVerticalLayout.StickyClipInset` = 56 identity row + 36 column row + 1 hairline (+48 with the Liked filter rail); the fill had simply been belt-and-braces over a cut that was already there. The ARTIST page had no clip at all (it relied entirely on the fill), so it gained one: `ClipTopAtViewport = ContextBandLayout.ClipInset` (56) on the magazine column AND on the blend wash, which lives in the same scrolling ZStack and would otherwise have slid its strongest top alpha straight through the band. Both engine binds SELF-GATE (released to `RectF.Infinite` until content reaches the line), so there is no state to keep in sync with the band's reveal ramp; the magazine's feather reuses the `compactInteractive` pin flag — the sentinel that raises it sits at exactly the magazine's top, so it flips on the same frame the clip engages and the feather costs zero extra page re-renders. **The one real regression the fill had been hiding**: the surface-colour scroll-edge cue paints an opaque gradient at the viewport's top edge whenever the scroller is offset — i.e. straight over the band — in a colour resolved by an ANCESTOR walk (`SceneRecorder.TryResolveCueSurface`). Both pages' grounds are ZStack SIBLINGS, so the walk sails past them to the shell's neutral ground: a one-rung-off slab over the band. ArtistPage had already opted out for exactly this reason and documented it; the two detail scrollers now do too. That needed one additive Controls change — `ScrollOptions.EdgeCues`, forwarded in `ItemsView.Create`/`CreateBound` beside the `AutoEdgeFade` pair it mirrors, because the knob existed on the `ItemsView` builder but not on the `ListOptions` path the app uses. **What still paints**: the hairline, the pivot's active underline, the text ink. Nothing else. Hit-testing is UNCHANGED and was never at risk — `InputDispatcher.HitAny` gates on `NodeFlags.Visible | HitTestVisible` and bounds, never on paint, so an unpainted row with handlers is a live target (the title bar's unpainted caption islands are the precedent); the row declares `HitTestVisible = true` explicitly so that stays legible. **Deleted**: `WaveeColors.ContextBand` and `ContextBandOver`, `ContextBand.Fill`, the band row's fill parameter, the tracklist chrome band's bound `pinned ? Fill : Transparent` brush, the selection arm's bound fill, the artist band's surface fill, and D38's entire page-tone hand-down (the `Signal<ColorF?>` on DetailShell, its TrackList and hero parameters, and the publish/clear effects in `CoverPageTonePlane`) — the hero-only fade never needed the tone, only the plane's geometry, so nothing survived for it. Pinned by an INVERTED `ContextBandLayoutTests.BandMaterial_IsNothing_TheBandPaintsNoFill` (the token is gone, neither arm assigns a fill, the row is unpainted AND hittable), a new `TheClipInset_CoversTheWholeBand` (the artist inset is the band height; the detail inset is that plus the column row and hairline exactly, grows with the filter rail, and both feather with the same band), a new `EveryBandSurface_ClipsItsContentAndOptsOutOfTheEdgeCue`, and a rewritten `DetailPageToneTests.TheBandShowsTheTone_RatherThanApproximatingIt` which asserts no tone hand-down survives anywhere and that the plane still precedes the page in the shell root's ZStack. |
| D40 | Fixed in code; gates green; manual acceptance retained | **The context band's section pivot was broken both ways: the accent underline never left "Top tracks" no matter where the page was, and clicking any pivot item — or Play / Following beside it — did nothing.** TWO independent defects, and each alone was enough to make the band look dead. **(1) THE REGISTRY WAS EMPTIED BY THE FRAME THAT FILLED IT.** The spy resolves "which section am I in" through a page-owned `SectionAnchors` registry that the reconciler fills via `OnRealized` — one node per pivot-visible section plus the scroll viewport. `ArtistPage` dropped that registry on identity change from a `UseEffect(…, routeKey)`. The ordering is fatal and it is not a race: `OnRealized` fires during the RECONCILE of the render that returned the tree (`Reconciler.Realize`), while `UseEffect` bodies run after PRESENT — so the mount effect wiped the registrations that same frame's realization had just made, and since a node realizes exactly ONCE nothing ever put them back. This was not a re-route edge case; it happened on the FIRST mount of every artist page. With `Viewport` null the spy bailed at its own guard and never wrote an index (underline stuck on item 0 — which read as "the scroll spy is stuck" rather than "the scroll spy is dead"), and every pivot click resolved a null node and returned (dead clicks). The reset now runs in RENDER, guarded by an `_anchorsRoute` compare — i.e. BEFORE the new nodes realize, which is the only correct edge for a registry the reconciler fills. `ContextPivot.GoTo` additionally checks `IsLive` on both the section node and the viewport (the spy's `Resolve` already did), so a registration that outlives its node can never scroll against a reclaimed handle. **(2) THE OFFSET MODEL'S CLIP WAS PAINT-ONLY, SO THE CLIPPED-AWAY CONTENT KEPT TAKING THE BAND'S CLICKS.** D39 made the band paint NOTHING and clip the content at its lower edge; D39's own note ("Hit-testing is UNCHANGED and was never at risk") was wrong about a case it did not consider. `InputDispatcher.Hit`/`HitAny` walk children first→last and keep the LAST match, and on every page with a band the guillotined body is by construction the LATER sibling — `ArtistPage`'s column is hero → sentinel → magazine, `DetailTracks.TrailingBody`'s is hero → chrome → body. So an invisible track row or album card sitting above the cut won every click aimed at the band, across its whole width: the pivot, Play, Following, and the detail band's Find / Filter / Play alike. The engine gained the missing INPUT DUAL, additive: `InputDispatcher.ClipRectAdmits` rejects a point above an engaged sticky cut in both walks — the same thing `NodeFlags.ClipsToBounds` already does for the box, and a clip is a scissor the recorder hands the whole subtree, not a paint (paint-derived hit-testing stays deliberately absent). It is scoped to the sticky cut by its sentinel sides, now the named `NodePaint.StickyClipSpan`: a FINITE `ClipRect` box is a reveal/flight presentation (an `AnimChannel.ClipL/T/R/B` ComboBox dropdown splitting open, a connected-animation flight) over a surface that is already logically live, and gating input there makes an opening menu reject the click that opened it — the broad version of this gate failed exactly that check. **The trailing-path follow-up D39 left open (album/show band clicks reaching clipped rows) is fixed by this same mechanism**, as is the discography drawer's own `ClipTopAtViewport` grid. **(3) A THIRD, SMALLER LIE, now removed**: `ContextBandLayout.ActiveSection` answered 0 when NOT EVEN THE FIRST section had a measurement, i.e. it published "you are in section one" from zero evidence — which is precisely why a dead spy was indistinguishable from a stuck one. It returns −1 there now (the live caller ignores negatives, so the mark HOLDS), and the empty-scan case keeps its own name in the tests. Pinned by a new VerticalSlice engine gate `23u3c` (a pinned band as the EARLIER sibling, a clipped body as the later one: a point inside the band resolves the band's button, a point below the cut still resolves the row — it fails on the pre-fix engine, resolving the row for both), and Wavee-side by `ContextBandLayoutTests.AScanThatLearnedNothing_HoldsTheLastAnswerInsteadOfSnappingToTheFirst`, `TheAnchorRegistry_IsResetDuringRender_NotFromAnEffect` (one reset, guarded by the route compare, not inside any effect body) and `TheStickyClip_GatesInputAsWellAsPaint` (the engine gate exists and is applied by BOTH walks — the app's band silently depends on it). |
| D42 | Fixed in code; focused tests green; live-server acceptance retained | **The daylist hero was yesterday's, and clicking through to the correct detail page and back did not fix it.** Three independent defects, and the report's four steps are one per defect plus the control. **(1) THE STORE HEADER WAS TRUSTED FOREVER.** A daylist Home card is SHALLOW by construction (`SpotifyExportMapper` marks `NeedsHydration` when `name` is empty or byte-equal to `daylist_pretitle`), so its rendered title and cover come ENTIRELY from `HomeDaylistHydrator`'s overlay of the resident playlist header — and `IStore.GetPlaylist` falls through to the SQLite cold tier (`CachedStore.GetPlaylist`), which persists any playlist that was ever opened (`PinnedPlaylist` → `_adopted`). So Sunday's header survived the restart, `TryExact` accepted it (it differed from the shallow title, which was the only test), and `OverlayCard` stamped it over BOTH the title and the server's fresh cover. The header network read only ran on a MISS, so nothing in the flow could ever correct it; the invalidating requery did fire (the identity was new to a fresh process) but the refreshed body is still shallow, so the stale overlay was simply re-applied on top of it. **(2) A PARKED PAGE ONLY RE-READ ON ITS OWN 60 s TICK.** KeepAlive park runs no cleanups (`Reconciler.SetSubtreeParked` detaches and flips `NodeFlags.Parked`; scopes are disposed only by `UnmountSubtree`), so the refresh loop survives a park — but the page's ONLY re-read trigger WAS that loop, and `UseActivation` re-published the wash and nothing else. Navigating back inside the tick window therefore rendered whatever the last tick produced, for up to a minute. **(3) NEW TAB WORKED, WHICH IS WHY IT LOOKED LIKE A CACHE BUG AND WAS NOT**: a fresh mount reads IMMEDIATELY, and by then the detail-page open had rewritten the store header — same shared `LiveHomeCache`, same hydrator; the whole asymmetry was "who does an immediate read". **THE FIX MAKES THE PLAYLIST REVISION THE STALENESS PRIMITIVE.** A daylist is one URI whose title, art and contents roll over behind a monotonically advancing playlist4 revision (4-byte big-endian counter + hash). Pathfinder `home` is GraphQL and carries no playlist4 field, so the revision has to be READ: `PlaylistFetcher.FetchPlaylistRevisionAsync` is a HEAD read — `GET /playlist/v2/{path}?decorate=revision`, the same decorate-revision idiom `RootlistOps.BootstrapRootlistAsync` already uses — that writes NOTHING to the store, and the hydrator issues exactly one per hydration-marked URI per Home read, coalesced across a 5 s window so a reactivation compare plus the read it triggers costs ONE call. The hydrator now remembers the revision the composed body REFLECTS (`_reflected`), and revision equality is the "already refreshed, do not requery again" answer: an unmoved daylist claims nothing, and a genuine rollover re-reads the header, spends one invalidating requery and steps `IdentityVersion`. Two weaker primitives were rejected and are not kept in parallel — a TTL on the header is a guess wrong in both directions, and the previous `(uri, title)` identity diff is a proxy that misses every rollover which keeps its title (pinned by `ARolloverUnderAnUnchangedTitle_StillRefreshes`). A FAILED probe reports `RevisionProbe.Unknown` rather than "no revision", so an unreachable spclient degrades to the previous answer instead of a per-read invalidate/requery storm. **AND THE REFRESH REACHES A PARKED PAGE, through ONE mechanism**: `Services.HomeFeedEpoch`, published by `LiveHomeCache` — bumped when a read observes a rollover (so every OTHER tab learns its card was superseded) and when the store rewrites the header of an already-hydrated identity, which is exactly what opening the daylist detail page does while Home sits parked behind it (a filtered `IStore.Changes` watch that reads nothing back, because it fires on the writer's thread). Home SUBSCRIBES to the epoch in its refresh EFFECT — never in Render — so a bump cancels the loop and starts a new one, i.e. one immediate re-read plus a fresh 60 s cadence, and because effects keep running through a park the parked page re-reads and its deferred render replays the fresh feed the instant it is activated. On REACTIVATION the page compares: an unapplied epoch re-reads once; an applied one spends only the head probe (`Services.HomeFeedRevalidate` → `LiveHomeCache.RevalidateAsync`), which resolves nothing itself — if the revision advanced it publishes the epoch and the ordinary effect does the read. `RefreshHomeOnce` is now ct-gated at the APPLY as well as the fetch, so a loop superseded mid-read cannot land its older feed on top of the newer one. KeepAlive, `SuppressLayoutTransitionsOnActivation` and the hero's D23 artwork-identity keyed cross-fade are untouched — a daylist rollover is a different `HomeWashPick.Key`, i.e. exactly the "different card cross-fades" case. Pinned by `HomeComposerDaylistHydrationTests` (10 cases: one head read per read and per URI not per occurrence; an unmoved revision costs one probe and nothing else; a newer revision re-arms the requery, steps the epoch, then stops again — the spent Sunday gate does not suppress Monday; rollover under an unchanged title; the reactivation compare reporting a rollover without resolving anything; the coalesce window sharing one head read; and a failing probe never refreshing). |
| D41 | Wave 8 landed (the immersive STAGE); build + tests green; manual acceptance retained | **D-number note: D40 is deliberately skipped** — a parallel fix on `main` is expected to claim it, so this wave takes D41 rather than racing for it. **The fullscreen surface was half a player.** `ImmersiveLyricsSurface` was a lyrics reader with a cover backdrop: it carried the track's name in 14/600 secondary ink at the top-left and NOTHING else — no transport, no seek, no queue — and its own header said so ("this surface carries NO transport or progress chrome … so the bar below has to stay reachable to pause/skip"). It has grown into the STAGE: a two-region fullscreen player over the same drifting σ80@0.5 `BakedBlur` backdrop. LEFT (`StageIdentity`) is a fixed bottom-anchored 352-DIP column — a 300 cover at `Radii.Card` with `Elevation.Dialog`, a 22/28/650 display-face title over a hover-underlined "Artist — Album" link to the artist page, the heart and the now-playing "…", the bar's OWN `SeekBar` with an elapsed/remaining pair, a 32·40·56·40·32 transport whose 56 filled circle is the only filled control on the surface, the stock volume slider recoloured, and an output-device line. RIGHT (`StagePanes`) is the same `LyricsView(large: true)` and a stage-skinned queue, CROSS-FADED in place, with a "Lyrics · Queue" text pivot in the bottom band. **Everything behind it is a reuse, not a fork**: `SeekBar` (its `Slider.DefaultStyle` derivation untouched), `TimeText`, `Slider.Create`, `PlaybackBridge` commands via the bar's own `ToggleShuffle`/`CycleRepeat`/`TogglePlayPause` intents, `PlayerBarContent.RemoteDevice` + `DevicePickerMenu` + `DevicePickerItems`, `Menus.NowPlaying`/`Menus.QueueEntry`, `QueueOrder`, `Reorderable`, `Surfaces.Artwork`, `WaveePalette.ChromeAccent`. **The material rule is the whole design**: the base scrim FLIPS with the theme (black@0.45 dark, white@0.62 light) because `LyricsView` paints `Tok.TextPrimary`, so the stage's chrome — which is theme-INVARIANT on-media white — could not sit on it. Each chrome region therefore brings its own always-dark VEIL derived from `Tok.MediaStage` (the caption strip, the identity column plus a 120-DIP falloff, the pivot band, and the queue pane's own floor while it is up), and the lyrics column keeps the base scrim untouched and keeps reading in both themes. `WaveeOnMedia` gained the missing GLASS rung (rest/hover/pressed at 0 / 0.10 / 0.16 of `Tok.OnMediaPrimary`) so an on-media control can have an interaction ramp without a resting plate. **The pane switch is opacity-only** — both panes stay mounted, `HitTestVisible` follows the active one, `MotionTok.ControlNormal` carries the fade and snaps under reduced motion — because a conditional mount would rebuild `LyricsView`'s measured document on every flip and change the component's hook shape between renders. Pane choice is a static session signal (last pane wins on reopen); the lyrics ticker now parks while the queue is up, so exactly one of the two mounted panes is ever ticking. **One structure, one reflow flag**: `StageLayout` is the engine-free ladder (one threshold at 600 with a 40-DIP promotion reserve, so a resize drag flips the stage exactly once per crossing), and below it the column folds to a 64-art header row with a 40 play and 32 steps while shuffle, repeat, volume and the device line move into a "…" that carries them all live. The seek is deliberately NOT on the fold list. **Two things were investigated and deliberately NOT built**: the times row's centre QUALITY BADGE mounts nothing — the pipeline computes the answer (`PlaybackEvent.SelectedBitrateKbps` / `AudioFormatName`) but only telemetry consumes it, neither `IPlaybackState` nor `PlaybackBridge` publishes it, and the two reachable alternatives (the user's quality PREFERENCE, and `ITrackExpansionService`'s per-track format ladder) both answer a different question, so the slot is empty rather than plausibly wrong; the ∞ AUTOPLAY row, by contrast, IS wired, because a real seam exists (`WaveeSettings.AutoplayEnabled` + `PlaybackPrefs.Epoch`, which `LiveSessionHost` binds into `PlaybackController.AutoplayEnabled`) and it is the same one the Settings toggle and the rail's queue pill drive. Pinned by `StageLayoutTests`: the derived threshold, one flip per upward AND downward sweep, the hysteresis asymmetry, monotone richness, exactly two shapes at any width, the two size ladders, the fold set and its in-row-XOR-in-menu complement — plus source scans for no theme-flipping `Tok.Text*` rung, no hand-mixed colour, no plate ladders, one filled control, the shared pivot rung constants (without escaping `WaveeCta.TextAction`'s context-band fence), opacity-only pane switching, the session-signal pane choice, the real autoplay seam, no caps transform, and an engine-free allocator. |
| D43 | Landed; gates green; manual acceptance retained | **The timeline showed half the notification feed.** Home's "New releases" module read the notification center's merged list and then kept only `NewReleaseNotification` — so the Spotify category's CONCERT/live-show announcements ("Just days away: … live in New York", "New … show just announced near you") were reachable only by opening the profile menu, opening the panel and picking the Spotify pill, even though they are dated news from artists the user follows and the module beside them is exactly a dated list of news from artists the user follows. **They now merge into the same day groups**, sorted by the instant the notification arrived — NOT by the concert date: a "days away" reminder is news about a future event, and filing it under the event would put a future row at the top of a spine that reads backwards from today. **The gate is a KIND, not the pill.** `NotificationCategory.Social` is a DISPLAY category that also holds followers and generic announcements; a surface outside the center that filtered on it would put "someone started following you" on Home. So the domain gained `SpotifyUpdates.KindOf` — the feed's own per-item discriminator when the payload ships one (now parsed as `SocialNotification.WireType`, three accepted spellings, none required), else the ACTION TARGET (`spotify:concert:` / `concerts.spotify.com` / a `/concert(s)/` web path), and nothing else; a title is never inspected, because a title is server-localized prose. Activity, app updates, followers and unrecognised announcements are not timeline material and cannot become it by accident. **Read state is now genuinely ONE store, in both directions.** The remote feeds' local read state was a per-feed WATERMARK, which is all a panel that marks everything on open needs and is useless to a surface marking ONE row seen. It gained its other half — `NotificationReadIds`, a bounded (200), append-ordered set codec over one persisted key, applied on top of the watermarks inside the ONE `NotificationMerge.Build` — so `NotificationCenterBridge.MarkRead(id)` moves the row's pip on Home, the row in the panel and the bell's badge together, and a watermark advance (Mark all read) CLEARS the set rather than accumulating beside it. There is no second read state anywhere: pinned by a source gate that fails if any file other than the bridge writes the key. **The click is the center's own**, verbatim — `NotificationPanel.ClickSocial` went `internal` rather than being copied, so the day either surface learns a new uri form (a concert uri that routes to `ConcertRoutes.Detail` in-app instead of the web player — the open follow-up) both learn it at once. **Row anatomy is unchanged**: `HomeCards.TimelineRow` now takes plain fields instead of a notification record (the row must not type-test), the concert leg wears the existing `Strings.Concerts.Detail.Concert` badge in the existing `KindTag` grammar (**no new loc string — the label already existed** and is already the eyebrow on `ConcertDetailPage`), a source line of the act's name where the feed named one (else `Strings.Concerts.LiveMusic`), and a ROUND 40-DIP avatar where a sleeve is square, exactly as the center draws the same two rows. Leading decorative glyphs are stripped from the title (`⏰ ` — the badge carries the semantics and the emoji throws the row's optical left edge out); casing is left alone, per D32's rule against case-transforming localized prose. **Two deliberate consequences.** The module now takes ONE signal read instead of two: the header's "N unheard of M" comes out of the merge, where it used to be the BELL's badge — which counted every unread notification in the app, app updates and follower rows included, and could therefore exceed the total it was quoted "of". And the row cap (8) is unchanged, so `HomePage`'s `HomeRow.Timeline` height estimate is untouched. With no concert items in the feed the module is what it was, row for row. Pinned by `HomeTimelineMergeTests` — the kind gate against a feed carrying all five notification types, the target/discriminator classification tables, an "empty Spotify category leaves the module identical" equivalence, arrival-not-event-date ordering, cross-source newest-first + one-day-one-group + local-midnight split invariants, deterministic tie-breaks, the cap-bounds-rows-but-not-the-counter split, the glyph strip's never-recase/never-blank rules, a shared-read-state round trip through `NotificationMerge`, the read-set codec's idempotence/bound/separator refusal, and two source gates (the timeline writes read state only through the bridge and holds none of its own; it takes exactly one signal read). |
| D45 | Wave 9 landed (the stage rework); gates green; manual acceptance retained | **D-number note: D44 is deliberately skipped** — a parallel light-mode wave on `main` is expected to claim it, so this wave takes D45 rather than racing for it. **The stage shipped as a two-world collage in light theme, with four layout bugs under it.** **(1) THE SCRIM HAD A THEME BRANCH, AND EVERYTHING ELSE WAS A PATCH ON TOP OF IT.** `ImmersiveLyricsSurface` painted a base scrim that FLIPPED — black @ 0.45 dark, WHITE @ 0.62 light — because `LyricsView` painted `Tok.TextPrimary`, which flips too. The stage's own chrome does not flip (it is on-media white in both themes), so every chrome region brought its own always-dark BOXED veil: an 88-DIP caption strip, the identity column plus a 120-DIP falloff, the pivot band, the queue pane. Over a white ground those veils are patches with locatable edges — the reported dark band across the top and the vertical dark-to-light smear at the column's falloff — and the theme-invariant WHITE title/artist, sitting past the column veil's falloff, landed on the near-white scrim and disappeared outright. **THE STAGE IS NOW SINGLE-THEME: ALWAYS ART-DARK, in both themes**, the way every art-forward player lights the room. The theme branch is deleted (a source gate now fails on `Tok.Theme`/`ThemeKind.` anywhere in the stage's files) and the veil patchwork with it (`TopVeil`/`BottomVeil`/`HeaderVeil`/`ColumnVeil` are gone by name). What replaced them is TWO full-bleed paint layers mounted once in the backdrop stack: `StageChrome.Scrim()` — ONE continuous vertical gradient over the whole body, deep at the top, deep at the bottom, with a genuine PLATEAU at the base value through the middle where the lyrics read — and `StageChrome.ColumnShade()`, one left-anchored layer that deepens the ground under the identity column. **Edge-invisibility is a mechanism, not a taste call**: every shade either reaches its own boundary at alpha ZERO after a long feather, or ends at a WINDOW edge where there is no outside to contrast with. The scrim's deepenings are FRACTIONS of the body height (0.22 / 0.62), so each feather is hundreds of DIP at any window size where the deleted top veil was a boxed 88; the column shade is a PAINT layer with no layout consequence, so it is free to be 612 wide against a 472 column BOX and to curve to exactly zero over 260 DIP — more than twice the layout gutter it used to be bounded by. The one local shade left is the queue pane's, which is genuinely local (it is mounted and cross-faded with the pane whose hover glass needs a floor) and comes up out of zero on its own left edge. **(2) THE LYRICS NOW PAINT ON-MEDIA INK, which is what let the scrim be one thing.** `LyricsView` was the only thing on the surface still speaking the theme, so it gained `LyricsInk` — a MODE, not a captured colour, resolved at the point of consumption, so the RAIL's copy still follows a live theme flip while the stage's is theme-invariant by construction. `new LyricsView(large: true, onMedia: true, …)` swaps the whole ladder onto `WaveeOnMedia`: the sung/unsung wipe pair, the held-note glow, the active/inactive line colours, the secondary translation layer, the interlude dots, the status message and the resync chip's plate + ring. The env-gated lyrics-search DEBUG panel deliberately keeps theme ink (it is a developer surface on an opaque theme plate) and is the one FENCED span in the new scan. **(3) THE SEEK BAR WAS A ~120-DIP STUB, THE VOLUME A DASH, AND THE TIMES PAIR COLLAPSED — one root cause plus a sibling.** The column's stack wrappers (the boxes that exist only to carry `WaveeEntrance.Row`) took the `BoxEl` default `Direction = 0`, i.e. a ROW — and a row hands its single child the child's INTRINSIC main-axis size instead of the column's 304. So the seek block measured to its own content, which is also why "0:15" and "3:20" printed together: the `Grow` spacer between them had no space to spread. Every wrapper is now `Direction = 1`, where the default `AlignItems = Stretch` gives each row the full span. The volume was a SECOND bug in the same row: `Slider.Create` takes a track LENGTH, not a stretch (its own call sites say so), and the `length: float.NaN` passed here propagated a NaN width through every part of the slider template. The track is now DERIVED — `StageLayout.ColumnContentW − WaveeCta.IconButtonSize − Spacing.S` — off the same 304 the rest of the column spans. **(4) THE CLUSTER WAS NOT BOTTOM-ANCHORED, THOUGH IT SAID IT WAS.** `WideColumn` declared `Justify = FlexJustify.End` and nothing else. A component's element is mounted UNDER a host node carrying the scene's DEFAULT layout (`LayoutInput.Default`: a column, `FlexGrow = 0`), so the column took its MEASURED height and sat at the TOP of a full-height host — `End` had zero free space to distribute and was silently inert. That is the reported "cover pinned top-left with a huge hole beneath it". `Grow = 1f, MinHeight = 0f` gives `End` something to end against; the compact header is untouched and still hugs its content at the top. The times row's structure was already the approved one (seek full-width, elapsed left / remaining right under it, centre slot empty until a quality-badge seam exists) — only its width was wrong. **(5) THE WHOLE COLUMN WAS ONE HOVER FAMILY: hovering the gap between two transport buttons lit the heart, and pressing anywhere lit every button's plate at once.** The mechanism is the engine's and is worth writing down. `WithContextMenu` sets `InteractionInfo.ContextBit`; ContextBit is in `InputDispatcher`'s hit-anywhere mask (an element with a context flyout is a hit target in its own right — the WinUI rule), so a pointer over the column's own BACKGROUND resolved the COLUMN as the hit. `AnimScheduler.SetHoverDescendants` then drives every descendant carrying a reveal/scale affordance — and every `StageChrome` button carries `HoverScale` — while `SetPressDescendants` is worse still: it recurses UNCONDITIONALLY, with no interactive boundary and no reveal filter, so one press on the container sets `PressTarget` on every descendant that has an interact row. The fix keeps right-click-anywhere and is structural: the menu now goes on a ZStack SHELL whose FIRST layer is a CHILDLESS full-bleed SHIELD. The shield always wins the hit over the shell's own box (`Hit` self-hits only when no child matched), and having no children its cascade reaches nothing; the shell keeps ContextBit as an ANCESTOR, which is what the "⋯" button's `ClickRequestsContext` funnel walk needs and what carries a right-click on the art/title/a button up to the menu — safe, because `HoverWithin` is published only for Pointer/Click/Pressed bits and the press target is the deepest HIT node. **The contract, pinned:** no stage container with several interactive descendants may own a pointer/click/press handler; handlers live on the leaf controls, and the childless shield is the one documented exception. Pinned by `StageLayoutTests`: the scrim's plateau + both feather fractions, the column shade overhanging the column BOX and its zero-terminating curve, the derived column content span, no theme branch in any stage file, the two full-bleed layers with no region veils left, every shade terminating at zero or a window edge, the column's Grow+End pair, every stack wrapper being a column, the derived volume track (and no `length: float.NaN`), the childless shield + exactly two menu attach points, the stage/rail ink split, and no theme ink anywhere in the lyrics reading surface outside the fenced debug span. |

## Manual acceptance still required

- D3: confirm one- and two-line Hero titles expose the full action row at the actual window tiers.
- D17: click, collapse, and switch artist pods; confirm the selected ring/rank state, height motion, and page-slide direction.
- D18: cold-start with a shallow Daylist; confirm the generic pretitle never flashes on successful hydration and no restart is required.
- D19: on both themes and all four palette presets, confirm the title bar → toolbar → sidebar → player dock read as ONE continuous ground with no seam, and that the content pane reads as exactly one step above it.
- D23: navigate between two Home states and confirm the wash cross-fades on a card change and re-colours in place when the same cover is re-graded.
- D24: light theme — confirm the chrome reads as Files-grey (#EDEDED) with the selected tab and content pane clearly lighter.
- D25: click every region of a tab (label, icon, padding) and library cards (cover, title, slack) — all must select; drags must still arm.
- D27: type a query letter-by-letter — first search shows row shimmers (never "…"), refinement keeps previous rows, results land promptly.
- D28: drag-resize across the full ladder (name → search → tabs → icon → overflow); tab drag-out + spring-load; caption drag / double-click-maximize / right-click-system-menu in the row's gaps; Ctrl+T and Ctrl+K (incl. re-press while focused); search suggestion popup from the title row; inactive-window dim; the sidebar band in all three designs + rail + drawer + customizer preview; hamburger↔compact-rail alignment (the +4/−4 nudge was dropped — eyeball it).
- D31: hover every tier — a track row / chip (Subtle), a Play pill or Follow pill (Standard), a card FAB and the transport (Emphatic) — and confirm the three read as three distinct loudnesses rather than one blur. Confirm the playlist-edit round icon buttons (share / collaborators / … / status) still read as pressable at the Subtle rung, and that a hovered card's shadow no longer looks like a floating flyout. With Windows “Show animations” OFF, confirm NO surface scales on hover or press anywhere in the app.
- D32: read one page of each family end to end. Home — the hero's four actions must read as one capsule row (accent Play, standard Shuffle, round heart, round …) and the greeting must show the user's name in normal case. An artist page — eight section headers, each with a 20×2 accent rule UNDER the title and no leading bar. A long tracklist — zebra still legible and hover still visibly different ON a stripe, in BOTH themes; the queue and friends rails must have no stripes at all. Any empty surface (clear the library filter, search for nonsense, open an empty queue) — big-type headline, no glyph, and the action must be grey, not accent. Concerts — select and deselect genre tokens: no label may shift and the strip must not reflow. Discography — breadcrumb, title and the Albums/Singles/Compilations SelectorBar; switching a facet must reload in place with the right item selected.
- D33: at the narrowest window the player bar keeps full-size transport buttons and a usable seek line; toggle shuffle/repeat and confirm the ON state is a plate + accent glyph (no dot) that is distinguishable from a hovered-but-off button; drag the volume thumb (it should be the same size as the seek thumb). Grab all four column splitters (library left, library mid x2, detail rail) — nothing should be painted at rest, the indicator should appear as soon as the pointer is anywhere in the strip and stay while dragging, and the collapsed detail seam must still re-open. Open the discography pane: "Go to artist" must read as a live link, not a disabled pill. Open a right rail and the immersive lyrics surface back to back — the header buttons must be the same size. Scroll an album page to the bottom: every trailing section must scroll with the page (no sideways gesture anywhere), and "Show all" must lengthen the section in place. Check one artist page's eight section rules sit tight under their titles and do not push the shelf chevrons out of line.
- D34: open Search with an empty box — the Browse title and its bands must cascade in once, top to bottom, and must NOT re-cascade when you scroll the directory. Type a query and switch to the Songs tab — the first rows rise in, the ones past the eighth land together, and scrolling the list must never replay an entrance. Turn Windows "Show animations" OFF and repeat both: everything must appear immediately, with no rise and no delay. Look at the tab strip in both themes — an inactive tab should read as ordinary secondary text (clearly brighter than before), come to full strength under the pointer, and its close × must no longer be dimmer than the selected tab's. Open Albums / Artists / Podcasts — each must open on its big-type name above the sort picker, on one line, with no clipping at the narrowest column; then narrow the window until the library collapses to one column and confirm the title is gone and only the breadcrumb names the kind.
- D35: scroll an artist page past the hero — the band must slide in as ONE opaque strip with the artist's name left, the page's section names beside it and Play / Follow right; no avatar, no capsules, no shadow, and no track/shelf content readable through it. Walk the whole page: exactly one section name is underlined at a time, it advances as each section passes under the bar and never flickers on a boundary; click a section name and the page glides so that section's header lands just below the band. Narrow the window down to phone width and back: section names drop from the RIGHT and come back in the same order, the name and the two actions never disappear, and nothing clips. Open an album and a playlist and scroll: the identity row and the column header (#, Title, Album, Time) must read as ONE surface with a single line under them — no gap and no strip of raw rows between them — and the old floating capsule / search glyph / accent circle must be gone. Click Find (the field takes the title's place, the actions stay put), Filter (the flyout opens, the word turns accent while filters are on) and Play. In light and dark, check the band's colour against the page beside it — it should read as the same surface, not as a plate. Finally, turn Windows "Show animations" OFF and repeat both pages: the band must fade in with no slide, and a section click must jump rather than glide.
- D38: open an album, a playlist and Liked Songs in both themes. The page must sit on ONE quiet colour taken from the cover — clearly the record's hue, clearly not the record's own brightness — with the blurred art melting into it behind the hero and gone by the first track row; a black-and-white sleeve must give a neutral page, not a random tint. Read the hero: artwork, then type/year, title, the 20×2 accent rule, artist, meta, an accent Play capsule leading four quiet 32-DIP satellites, then the description — the SAME parts in the SAME order at every width. Drag the window from wide to phone-width and back: the artwork must move from beside the copy to above it exactly once per sweep (never flicker on the seam), the title must step through three sizes and no other size, and nothing must change ink, plate or button shape. Scroll: the hero dissolves into the context band, and the band must read as part of the tinted page rather than as a grey strip on it. Turn Settings ▸ Appearance ▸ “Limit page color to the hero” ON — the colour must fade out under the hero with the list on the plain surface — and then “Disable color washes” — the page must go fully neutral, band included. Finally with Windows “Show animations” OFF: the hero's copy must appear immediately with no cascade.
- D39: scroll past the hero on an artist page and on an album, a playlist and Liked Songs, in both themes and on a DARK wallpaper (the case that produced the report). The sticky band must never read as a plate: on the artist page it must be the same material as the page around it, separated only by its hairline; on the detail pages it must carry the record's own tone and the top of the blurred cover, and must NOT change colour as you scroll (the tone plane does not move). Nothing may be readable inside the band — walk a long tracklist slowly and confirm rows dissolve at the band's lower edge rather than sliding through it or being sliced with a hard line, and that the cut sits exactly under the column header, not above or below it. Cross the collapse point up and down repeatedly: the cut must engage and release with the band and never leave a stripe of clipped content behind. Confirm no grey or black gradient appears at the top of the viewport while scrolled (the scroll-edge cue). Click Find / Filter / Play in the detail band and Play / Following plus every section name in the artist band — all must respond, and the wheel over the band must still scroll the page. Finally turn Settings ▸ Appearance ▸ "Limit page color to the hero" on and confirm the band still shows the tone with no seam at its lower edge.
- D37: at a window narrow enough to collapse the search to the magnifier (roughly 900-1600 DIP with several tabs open), click it: the field must open to at least 240 DIP IN PLACE, with the profile name, then the Friends button, then tabs visibly folding away to pay for it — and the "v" badge count must agree with what left the strip. Type, then press Escape twice: the query clears, the field collapses, and every folded affordance comes straight back (Friends must be reachable in the profile menu the whole time it is out of the row). Click the magnifier and immediately drag-resize the window in both directions — no jitter, no chrome flapping, and the row must never overflow into the caption buttons. Ctrl+K must do the same thing as the click, including from a page that already has focus. Then check the two edges: at a window under ~800 DIP the magnifier stays a magnifier (the click yields only a small field - known and documented), and at a window wide enough for a permanent field the latch must do nothing at all.
- D42: open the app on a day AFTER one where you opened your daylist. Home's hero must be TODAY's daylist — title, tags and artwork — on the first frame it has content, with no stale flash and no restart. Then walk the reported flow: click the hero (the detail page opens on the current daylist), navigate BACK, and confirm Home shows the same current daylist immediately, not the previous one and not after a wait. Open a second tab and confirm both tabs agree. Leave Home open across a daylist rollover (it retitles through the day) and confirm the hero and the shell wash CROSS-FADE to the new card rather than snapping or staying put. Finally pull the network briefly and navigate away from and back to Home: the hero must keep showing what it had, and must not flicker or re-request in a loop.
- D43: with a Spotify account whose notification center holds concert announcements, open Home and read the "New releases" module. The concert rows must sit in the same day groups as the releases, in the same anatomy, with a "Concert" badge and a round avatar, no leading emoji in the title, and the header's "N unheard of M" must count them. Click one — it must go exactly where clicking the same row in the notification center goes — then open the profile menu → Notifications: that row must now read as READ there too, and the avatar's unread badge must have dropped by one. Do it the other way round as well: "Mark all read" in the panel, then back to Home — every timeline pip must be hollow and the counter gone. Finally check an account with NO concert notifications: the module must look exactly as it did before (no empty day groups, no new chrome), and one with followers/social items only — none of those may appear on Home.
- D40: on an artist page, scroll past the hero and walk the whole page slowly. The pivot's accent underline must ADVANCE as each section passes under the band (Top tracks → Singles & EPs → Albums → …) and must never sit on a section you are not in; scroll back up and confirm it walks backwards the same way. Click every pivot item — each must scroll that section's heading to just under the band — and click Play and Following in the same band. Then do the same in an album / show band (Find, Filter, Play) and confirm a click that lands on the band never triggers the track row hidden behind it. Cross the collapse point repeatedly and click immediately after the band appears. Navigate artist → artist (a related-artist card) and repeat: the pivot must work on the NEW page without a reload. Finally confirm the wheel over the band still scrolls the page, and that a ComboBox / flyout still accepts a click on the item you press the instant it opens.
- D45: open the stage with music playing, in LIGHT theme and on a BRIGHT sleeve (the reported case), then repeat in dark. The surface must look IDENTICAL in both themes: one art-dark room, no white anywhere, no dark band across the top, no vertical dark-to-light smear beside the identity column, and no locatable edge on any veil — sweep your eye across the column's right-hand falloff and down past the caption strip and the pivot band and you must not be able to point at where any of them ends. The title and "Artist — Album" must be plainly readable (they were invisible in light theme). Read the lyrics: active line bright, past/upcoming lines dimming away, the karaoke wipe's sung/unsung boundary still legible, the interlude dots and the resync chip readable — all of it white-on-dark in BOTH themes, while the RAIL's lyrics panel must still follow the theme as before (open it in light theme and confirm dark text on the panel). Then the geometry: the seek bar must span the whole column (~304 DIP) with the elapsed time hard left and the remaining time hard right UNDER it, the volume rail must span the column minus its mute glyph, and the whole cluster — cover, title, seek, transport, volume, device — must sit anchored LOW with the empty room above it, not top-left with a hole beneath. Finally the hover/press scope: move the pointer slowly through the GAPS between the transport buttons — nothing anywhere in the column may light up — and press and hold on the column background, on the cover, and on each button in turn: exactly ONE control may show a pressed plate, the one under the pointer. Right-click the cover, the title, a transport button and the empty column background — all four must raise the same now-playing menu — and click the "⋯" button, which must raise it too. Drag the window edge across 600 DIP and back: the compact header must still fold correctly and its seek must still span the header.
- D41: open the stage (the expand button in the rail's lyrics header) with music playing, in BOTH themes and on a bright, light-coloured cover. Read the left column top to bottom: a 300 cover with a real shadow, the title on one line, "Artist — Album" underlining itself when you hover it and opening the artist page when you click it, the heart filling with the cover's accent when you save, the seek with elapsed left and remaining right and NOTHING between them, then shuffle · previous · a white filled play · next · repeat, then volume, then the output device. Nothing on that column may be hard to read on a white sleeve in light theme — that is what the column's dark veil is for. Toggle shuffle and repeat: each must latch with an accent glyph on a faint plate, and must agree with the player bar below. Click the device line: the same two-section picker the bar's Devices button opens. Play to another Spotify Connect device and confirm the line says "Playing on …". Now the panes: click "Queue" bottom-right — the lyrics must FADE into the queue in place (nothing slides, nothing pops) and the accent underline must move to Queue by changing colour, not by flying. In the queue, confirm the "Playing next" header with "from …" beside it, the 20×2 accent rule under it, the ∞ Autoplay row (turn it off and back on — the Settings ▸ Playback toggle and the rail's queue pill must both agree, and the autoplay tail must appear/disappear), then 56-DIP rows whose grip appears on hover; drag a row in the user-queue section to reorder it, right-click for Play now / move / remove, and drop a track from the sidebar into it. Click a row: it must skip in place. Switch back to Lyrics, close the surface with Escape and re-open it — it must come back on the pane you left it on. Confirm the window caption strip and the docked player bar are still live and hittable the whole time. Drag the window narrow past ~600 DIP and back: the column must fold to a header row (64 art, 40 play, 32 prev/next) with shuffle, repeat, volume and the device picker all present inside the "…", the seek must survive the fold, and the flip must happen exactly ONCE in each direction with no flapping on the seam. Finally with Windows "Show animations" OFF: the column's contents must appear at once with no cascade, and the pane switch must snap rather than fade.
