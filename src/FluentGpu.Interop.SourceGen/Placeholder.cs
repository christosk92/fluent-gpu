namespace FluentGpu.Interop.SourceGen;

/// <summary>
/// SCAFFOLD (the COM-binding generator — see design/subsystems/com-interop.md). Emits hand-vtable <c>IComObject</c>
/// consume + callee CCW vtables from a harvested <c>*.comabi.json</c> (no human-typed slot indices). Leaf-only.
/// </summary>
internal static class InteropSourceGenMarker;
