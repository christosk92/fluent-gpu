namespace FluentGpu.Animation;

/// <summary>
/// SCAFFOLD (see design/subsystems/backdrop-effects-animation.md). The composition-style animation phase (phase 7):
/// transform/opacity timelines + springs (velocity-integrating AnimTrack) + retarget + animateContentSize +
/// connected/implicit transitions + DrivenClock — all Transform/PaintDirty, never LayoutDirty.
/// </summary>
public static class AnimEngineMarker;
