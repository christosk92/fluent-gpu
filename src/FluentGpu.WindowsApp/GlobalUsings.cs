// The showcase-app scaffolding lib (WS7). A global using so the ~100 gallery pages resolve the graduated chrome
// (ExampleCard, CopyButton, RelatedLinks, PageHeader, GalleryScaffold) and the [GalleryPage]/[Sample] attributes +
// Knobs/Sample records by their short names — the same convenience the pages already have for FluentGpu.Controls.
global using FluentGpu.GalleryKit;
