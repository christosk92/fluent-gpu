# Adversarial review â€” Wavee persistence redesign

Verified against code at the cited anchors. All file paths under `C:\wavee\fluent-gpu\`.

## CONFIRMED issues

1. **Pin-gate + no-op elision permanently strands entities that become pinned after their last write (offline-first break).** The gate (Â§A.4.1) is evaluated only inside the six `Upsert*` chokepoints, but pinning happens through paths that never call `Upsert*`: liking (`src/apps/Wavee/Backend/Mutation.cs:61` â†’ `store.SetSaved`), adding to a playlist (`PlaylistMutationSource`), delta adds (`Collections/CollectionDeltaApplier.cs:28`). Scenario: a track hydrated from queue/radio/browse (Phase-1 "hot-only" â€” never cold-persisted) is then liked. `SetSaved` writes only `collection_items`; no entity upsert fires. Worse, when a later SWR hydration *does* re-upsert it, Â§A.4.2's 64-bit payload-hash elision sees "unchanged" and skips `_cold.UpsertEntity` â€” the row never reaches disk. After restart, `GetLikedSongsAsync`'s inner join silently drops it (`Backend/Library/StoreLibrarySource.cs:268-269` "a not-yet-hydrated member has no row"). The design needs an explicit pin-transition flush (on SetSaved/SetMembership/override-attach: persist the hot entity if cold lacks it) and the elision hash must encode cold-tier presence, not just payload equality. Same root cause hits the closure gate: at `UpsertAlbum` time "referenced by a pinned track's AlbumRef" requires a maintained reverse set; if the album projects before its referencing track registers, the album write is hot-only and elision blocks the retry.

2. **"WAL: readers never block" is false in this codebase â€” every read and write serializes on one `_connLock` over a single connection.** `SqliteColdStore` guards *all* access with `lock (_connLock)` (`SqliteColdStore.cs:196, 229, 257, 296, 311, 366, 396, 590`). The Phase-2 background `WarmAsync` bulk query, the v6 GC's 1000-row DELETE transactions, and `incremental_vacuum` slices all hold that lock, and every synchronous UI-facing read â€” `ColdFallback` (`CachedStore.cs:125`), `Rootlist()` â†’ `LoadRootlist`, `Membership()` â†’ `LoadMembership`, `GetPlaylistRevision` â€” blocks behind them. First shell render after a fast silent-resume does ~216 `ColdFallback` point reads (liked join) plus rootlist/membership loads; if the warm bulk query or a GC batch is mid-flight, those stall for its full duration. The design's C.6 "WAL: readers never block" premise requires a second read-only connection (or a connection pool), which it never specifies.

3. **The warm pin head-set (~21kâ€“31.5k rows) exceeds the hot tier's hard backstop (12k), so "warm = pin set resident" cannot hold.** `Backend/Store.cs:293`: `BackstopHigh = 12_000, BackstopLow = 8_000`; the backstop is explicitly **pin-set-free** (`Store.cs:289-292`, `MaybeBackstopEvict` â†’ `EvictCore(pinned: null, â€¦)` at `Store.cs:400`). Once the 60s `BackstopProtectMs` window lapses and any post-warm upsert churn occurs (InitialHydrate, playlist open), hot is shed to 8,000 entities regardless of pins. Consequences: (a) `InMemoryStore.QueryTracks` (offline search fallback, `StoreLibrarySource.cs:289`) and `LibrarySearchIndex.Run` (offline library search, `StoreLibrarySource.cs:297-300` â€” a second gated consumer the design doesn't name) are only briefly complete after `WarmComplete`, then silently lose most of the library; (b) at 100k tracks, warming 155k rows into hot is both a memory spike (~100+ MB by the store's own byte model, `Store.cs:286`) and futile. The design must either raise/redesign the backstop or drop the "QueryTracks awaits WarmComplete â†’ correct" claim; the honest fix is Phase 3's index-backed search, pulled earlier.

4. **`GetVideoAssociation` has no cold fallback, and step 4 removes its eager load â€” pre-warm reads return wrong answers.** `CachedStore.cs:115`: `GetVideoAssociation(uri) => _hot.GetVideoAssociation(uri)` â€” hot-only, unlike the six entity kinds. Design step 4 deletes the ctor `LoadAllVideoAssociations` replay (currently `CachedStore.cs:39`) and defers it to the warm task, so during the window between first frame and warm completion, has-video answers are false-negative. Given the documented fragility of the video kind ping-pong at track edges (video-track-end-deadlock), a resume-into-a-music-video during that window flips the placement machine to audio-only and back. Needs either a `ColdFallback` for `video_assoc` or keeping its (small) eager load in step 4.

5. **`DefaultIgnoreCondition = WhenWritingDefault` corrupts `Playlist.IsPublic`.** `Wavee.Core/Domain/Models.cs:283`: `bool IsPublic = true` with the doc "authoritative; default true until fetched". `WhenWritingDefault` omits `false` (== `default(bool)`), and deserialization of the missing member re-applies the constructor default `true` â€” every **private** playlist round-trips through the cold tier as **public**. Any non-nullable member whose constructor default â‰  `default(T)` is a landmine of this class (`DiscCount = 1` at `Models.cs:188` is currently unreachable-at-0 only by merge-invariant, `Store.cs:150-152`). The design adopts the option globally with no audit; it needs a per-type audit or `[JsonIgnore(Condition)]` opt-ins instead.

6. **The pin set over-pins: `playlist_items` contains every playlist ever *opened*, not the user's library, and nothing ever reclaims it.** `ReplaceMembership` (`SqliteColdStore.cs:394-431`) runs for any adopted snapshot â€” `PlaylistFetcher.FetchPlaylistAsync`/`AdoptSnapshot` fire on opening *foreign* playlists too â€” and the only `DELETE FROM playlist_items` in the codebase is that method's own per-playlist replace (`SqliteColdStore.cs:399`). Unfollow (`RootlistOps.cs:174`, `PlaylistMutationSource.cs:245`) never purges membership rows. Under Â§A.3 P0 (`SELECT item_uri FROM playlist_items`) all of those tracks are pinned **forever**, exempt from TTL and the 64 MiB budget. A user who browses large editorial playlists accumulates unbounded pins, and the size math (which equates pins with the ~20.7k own-library membership) undercounts. Needs a membership-GC rule (e.g., playlists âˆ‰ rootlist âˆ§ not recently opened â†’ membership rows demoted or deleted).

7. **Phase-1 hot-only routing of `DiscographyPrefetcher` deletes its stated product feature â€” offline discography for saved artists.** The prefetcher's contract is "everything persists via CachedStore â†’ SQLite, so next launch paints the discography offline" (`Backend/Metadata/DiscographyPrefetcher.cs:13-14`). Wave-2 album cards of a saved artist are neither in `collection_items` nor referenced by any pinned track's `AlbumRef`, so under the pin gate they are never persisted. The design's cited mitigation ("on-open heal", comment `:49-51`) is about *failed batches healing online*; offline there is no heal â€” the artist page shows a core header with an empty discography. `artist_overview` refs don't fix it: `ArtistDiscography.Assemble` (`:47-49`) upgrades stubs from *standalone album rows*, which won't exist. Either pin saved artists' own-discography cards (bounded: ~10â€“20 per saved artist) or accept and *state* the regression.

8. **v8 is not rollback-safe as claimed.** Â§E.4 drops `localized_entities` after the copy. A pre-v8 binary's `LoadAllEntities`/`GetEntity` query `localized_entities` directly (`SqliteColdStore.cs:203-215, 236-243`) â€” after the drop they throw `SqliteException` at startup; the fmt=0 prefix only protects a *v8-aware* build from decode issues, not a real binary rollback. "Each phase reversible" is overstated for v8 (v5 is genuinely rollback-safe since old binaries read the union; v8 is a one-way door unless the drop is deferred a release).

9. **Size-math discrepancies vs the measured averages.** Given measured payload averages (entities ~809 B, localized ~735 B, extensions ~736 B): (a) the design's inputs are 860 B and 880 B â€” both inflated vs measurement (extension cap: 4096Ã—736 â‰ˆ 3.0 MB, not 3.6 MB); (b) the closure arithmetic contradicts itself: "~0.25 artists per track" Ã— 21,000 = 5,250, but the table says ~3,500 (the 31,500 total silently uses ~0.167); (c) the ~200 B compressed payload assumes 3â€“4.3Ã— zstd on a ~600 B input â€” realistic only with the trained dictionary (fmt=2), which is labeled "optional/lazy" in Phase 3; without it, sub-1 KB JSON typically gets ~2Ã—, putting the entity table nearer 15â€“16 MB (total ~30 MB â€” direction fine, headline optimistic); (d) the Â§A.2 covering index `ix_entity_list(uri, title, subtitle, image_url, duration_ms, flags)` duplicates every scalar column (~+4 MB at 31.5k rows, ~+19 MB at 155k) and is **absent from the math** â€” and with payload-last rows of ~200â€“400 B nothing overflows a 4 KiB page anyway, so the PK point read already avoids payload I/O and the index buys almost nothing; (e) the 1M "no code path is O(total rows) on the paint path by construction" claim is contradicted by step 4 itself: `LoadAllSaved` eager-load + per-row `_hot.SetSaved` (`CachedStore.cs:41`) is O(saved set) before first paint â€” 1M liked tracks = 1M rows and hundreds of MB of hot saved-set state pre-paint.

## PLAUSIBLE concerns (not fully verified)

10. **BuildPinSet thread-affinity at GC time.** `Services.BuildPinSet` (`Services.cs:202-212`) reads signal Peeks plus `LibraryStore.CollectPinnedUris`; the governor deliberately marshals its caller to the UI thread because the detail caches are UI-thread-affine (`WaveeApp.cs:154-156`). Â§C.2 hands the "exempt list" to a GC pass running on the writer thread â€” if it calls `BuildPinSet` there, it races the detail caches. Must snapshot on the UI thread and marshal; the design doesn't say.

11. **Pin-snapshot staleness across batched GC.** The pin temp table is materialized once at GC start; a playlist adopted mid-GC pins entity rows that are old (outside the 15-min grace) and were unpinned in the snapshot â€” a later delete batch can remove them, leaving membership rows referencing missing entities until an online heal.

12. **Phase-0 expired-extension sweep vs the ETag contract.** `ExtensionEtagCache` deliberately seeds *expired* rows with `needsRevalidate: true` so revalidation is a 304, not a refetch (`ExtensionEtagCache.cs:74-77`). The Phase-0 "expired-extension sweep at open" as tabulated has no grace; Â§C.3's +7d grace exists precisely for this. If Phase 0 ships graceless, first go-live after the sweep pays full refetches for the whole working set.

13. **v5 mislabels legacy `entities` rows as the current locale.** Production always passes a locale (`Services.cs:295` â†’ `locale.SpotifyLanguage`), so `entities` rows are pre-v3 legacy of unknown fetch-locale; stamping them `$locale` preserves today's *serving* behavior (priority-1 leg served them anyway) but lies to any future locale-switch logic. No data loss found; the shadowed-row conflict analysis in the design checks out. Implementation nit: `INSERT â€¦ SELECT â€¦ ON CONFLICT DO NOTHING` needs the SQLite `WHERE true` disambiguation â€” use the codebase's existing `INSERT OR IGNORE` idiom (`SqliteColdStore.cs:106`).

14. **`WHERE uri IN (â€¦)` at scale.** The warm query and the JSON-closure pin query at 100k+ libraries exceed SQLITE_MAX_VARIABLE_NUMBER (32,766 in the bundled 3.x) â€” need chunking or the temp-table the design specifies only for GC.

15. **Â§H's single-file rationale defends atomicity that doesn't exist today.** Membership writes are synchronous own-transactions (`SqliteColdStore.cs:394-431`) while entity writes ride the async write-behind batch â€” "playlist adopt = membership + entity writes stay atomic" is already false. Doesn't invalidate the single-file conclusion (the pragma/è¿žæŽ¥-management and post-GC-vacuum-cheapness arguments stand), but the stated reason is wrong.

## SIGN-OFFS (verified, held up)

- **All cited anchors are accurate**: `CachedStore.cs:33-42/38/80-132/124/212/215-243/247-252/260-268`; `SqliteColdStore.cs:88-191/193-223/227-251/260-261/494-505/502/511-513`; `Services.cs:202-212/295-296`; `FluentApp.cs:114-147/149/191`; `WaveeApp.cs:215-221`; `MetadataService.cs:85-108`; `ExtensionEtagCache.cs:64-78`; `DiscographyPrefetcher.cs:20-47` + `LiveSessionHost.cs:145-146/346/349-358`; `LiveContextResolver.cs:354`; `ExtendedMetadataSource.cs:334/370-375/378`; `ArtistDiscography.cs:40-61`; `Store.cs:160-189`; `Models.cs:194-213`; `Wavee.csproj:93/95-99`.
- **Phase 0 `BeginBulk` ctor wrap is sound**: replay at `CachedStore.cs:38` is indeed un-bulked; `MaybeBackstopEvict` is suppressed in bulk and `EndBulk` fires one `StoreChange.Bulk` + one backstop pass (`Store.cs:389-401, 663-679`); no subscribers exist during ctor. The "~450 scans" arithmetic checks out ((127kâˆ’12k)/256 â‰ˆ 449), and they are indeed no-ops during replay (60s protect window).
- **`LIMIT` on `LoadAllExtensions` is safe**: newest-first ordering confirmed (`SqliteColdStore.cs:261`), and the cache's oldest-first seed loop (`ExtensionEtagCache.cs:65-78`) keeps semantics; note the live `maxEntries` is 2048 (default at `LiveSessionHost.cs:145`), so match the LIMIT to that, not 4096.
- **Write-ordering claim for the gate is correct as far as it goes**: membership/saved land before hydration (`PlaylistFetcher.cs:43-44, 172-177`; `CollectionFetcher.cs:64-65, 81-84`) â€” the failure mode is post-hoc pinning (issue 1), not initial-sync ordering.
- **Offline-critical tables are untouched by all migration steps** â€” v5â€“v8 touch only `entities`/`localized_entities`/`localized_extension_cache`/new tables; `collection_items`, `playlist_items`, `playlists.base_rev`, `rootlist`, `outbox`, `dead_letter`, `video_override` are never written by any step. Outbox pinning covers in-flight mutation targets (`outbox.entity_key`, `SqliteColdStore.cs:68`).
- **AOT feasibility holds**: STJ source-gen already in place (`CachedStore.cs:260-268`); ZstdSharp.Port 0.8.6 already referenced (`Wavee.csproj:93`), pure managed; SQLitePCLRaw bundle 3.0.0 ships a modern SQLite with JSON functions built in, so the v6 `json_extract`/`json_each` closure is available; no reflection anywhere in the proposal. protobuf-net rejection is correct.
- **Startup sequencing**: first authenticated-off frame genuinely needs zero entities (`WaveeApp.cs:215-221`); `root()`-before-`Show()` confirmed (`FluentApp.cs:149/191`); `InMemoryStore` cross-thread upsert safety claim confirmed (`Services.cs:22-24` comment + `_gate` locking throughout `Store.cs`).
- **Pin-set coverage for restart surfaces**: no persisted queue/now-playing/recently-played restore path exists in the app (grep-verified) â€” Connect state is remote-first, SMTC reads the in-memory bridge â€” so `recent_surfaces` + P0 + the in-memory exempt set is complete for what actually survives restart *today* (issues 1/6/7 aside).
- **Derived pins (no stored boolean), unpinâ‰ purge, TTL-filter/LRU-ranker, day-granularity batched touch, auto_vacuum activation via one full VACUUM, WITHOUT-ROWID rejection for ~1 KB rows, and the v5 `updated_at=0` priority-parity argument** all check out against the code and SQLite semantics.