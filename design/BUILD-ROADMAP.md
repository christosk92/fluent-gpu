# fluent-gpu — Build Roadmap (design → gap-free implementation)

Generated by a 12-agent gap-analysis workflow (design spec vs actual `src/` code), synthesized into one
dependency-ordered plan. **Verdict: the current code is ~5% of the spec.** Reaching a gap-free framework is the
37 steps below, ordered strictly by dependency then value (shared contracts first). Steps marked **needs-pixels**
have correctness that can only be confirmed on-screen / with a real GPU or assistive tech — not headlessly.

**Critical path:** 1 (harness+sln) → 2 (Foundation allocators/handles) → 3 (Foundation value types/flags/deps) →
5 (Scene 64B/96B columns) → 6 (interning/realization) → 7 (SceneStore rebuild + dirty worklist + full ISceneBackend)
→ 8 (threading/publish seam shapes) → 9 (decouple D3D12 leaf) → 10/11 (DrawList+batcher; sRGB+offline DXC) →
12 (RHI handle table + PSO cache + device-lost) → 13 (Layout substrate) → 16 (ported-Yoga verbatim) →
17/18 (text measure seam + DirectWrite shaping leaf) → 20 (source-gen toolchain) → 21 (full reconciler) →
23 (13-phase loop) → 24 (render-thread split) → 25 (incremental layout) → 26 (pixel-snap) → 33 (parity/golden gates).

## Status legend
✅ done & verified · 🟡 partial (capable subset; not yet spec-exact) · ⬜ not started · 🖼 needs-pixels

| # | Subsystem | Status | Build (one-line) |
|---|-----------|--------|------------------|
| 1 | Test/CI harness + `FluentGpu.slnx` | 🟡 | xUnit `tests/` tree + BDN + FakeSceneBackend + alloc-tripwire + SizeOf golden. *(Today: the VerticalSlice runs 18 headless assertions incl. zero-alloc.)* |
| 2 | Foundation — allocator + handle substrate | ⬜ | `ChunkedArena` (reserve/commit, stable addrs), `HandleTable<T>` (dispose-before-reclaim), full typed-handle family. |
| 3 | Foundation — value types/flags/deps/lanes | 🟡 | `NodeFlags` full map, **`DepKey` 16B scalar**, `Lane`, `LengthValue`, `[InlineArray]` Edges4/Ring8, `Affine2D.Invert`. *(Today: `FlexJustify`/`FlexAlign`, geometry, `Diag`.)* |
| 4 | Pal/Rhi — POD vocabulary widening | 🟡 | 40B `InputEvent`, full `InputKind`, `WindowEvent`, widened RHI descs/encoder; PAL seam shells (clipboard/IME/colors/backdrop/video/codec). *(Today: `InputEvent` carries `KeyModifiers`/`PointerKind`/`IsRepeat`/`TimestampMs`/button; `InputKind` += KeyUp/PointerCancel/WindowBlur/WindowFocus; full `Keys` VK table incl. gamepad; Win32 pump: R/M buttons, KEYUP, modifier capture, `SetCapture` drag streaming, WM_ACTIVATE blur, WM_SETCURSOR + full `CursorId` set; slice E2.a–E2.l.)* |
| 5 | Scene — column structs to spec shape | ⬜ | `NodePaint`=64B, `LayoutInput`=96B (`LengthValue`+`LayoutPacked`), identity/a11y/focus/selection/scroll columns. |
| 6 | Scene — interning + realization tables | ⬜ | Brush/Clip/GlyphRun/Image/EffectChain tables (content-hash dedup, epoch-stamped) + `EffectAux`. |
| 7 | Scene — SceneStore rebuild + dirty worklist + full `ISceneBackend` | 🟡 | Mutate chokepoint, two-phase growth lock, `DirtyWorklist` (O(0) idle), WorldTransform, QuarantineLedger, full POD seam. *(Today: SoA spine, CreateNode/Free/Append/**Detach**, ref accessors.)* |
| 8 | Hosting/Threading — publish seam shapes (single-thread) + `ThreadGuard` | ⬜ | `SceneFrame` snapshot, triple-buffer publisher, `QuarantinePolicy`, DrawList arena ring. |
| 9 | Windows/D3D12 — decouple leaf from engine | ⬜ | Move DrawList decode + DWrite shaping out of the leaf into `Render`; leaf consumes POD batches + handles only. 🖼 |
| 10 | Render — DrawList encoding + SortKey + batcher | 🟡 | 8B `DrawCmd` header + ~30 opcodes, `SortKey64` + stable radix, overlap-aware batcher. *(Today: int-op stream — FillRoundRect/DrawGlyphRun.)* |
| 11 | Windows/D3D12 — color contract + present pacing + offline shaders | 🟡 🖼 | `_UNORM_SRGB` RTV + linear-premul brushes + gradient atlas; frame-latency waitable; **offline DXC→DXIL** shaders. *(Today: runtime D3DCompile, premultiplied output, DComp present.)* |
| 12 | Windows/D3D12 — device/swapchain split + handle table + PSO cache + device-lost + texture upload | 🟡 | Multi-window device, `RhiHandleTable`, PSO cache, device-lost recovery, staging ring. *(Today: single device+swapchain, fence sync, WARP fallback, glyph-atlas upload.)* |
| 13 | Layout — substrate (aux slab, node view, scratch, cache ring) | ⬜ | `LayoutAux`, `LayoutCacheEntry` Ring8, `LayoutNode`/`LayoutContext` ref-structs, non-relocating `LayoutScratch`. |
| 14 | Hooks — DepKey runtime + core hook zoo | 🟡 | `GcDepTable`, `ReadOnlySpan<DepKey>` overloads, Context cells, effect-timing split. *(Today: UseState/Reducer/Memo/Ref/Effect/**LayoutEffect**, `object[]` deps.)* |
| 15 | Dsl — Element model + modifier arena | ⬜ | `ModifierOp`/`ModifierRef` arena, `ChildList` over `ChunkedArena`, composition wrappers, authoring attributes. |
| 16 | Layout — ported-Yoga FlexLayout (verbatim numerics) | 🟡 | Yoga 11-step body verbatim (wrap/aspect/align-content/reverse/freeze-fixpoint) + golden parity oracle. *(Today: capable flex — grow/shrink/justify/align/stretch/margins/min-max; golden checks 10–13.)* |
| 17 | Text — full portable seam + `ITextLayoutEngine` measure dispatcher | ⬜ | Full COM-free seam + PODs; ring-probe-first MeasureKind dispatcher; `MeasureMode`. |
| 18 | Windows/DirectWrite — shaping/itemize/fallback leaf + portable atlas | 🟡 🖼 | itemize (BiDi/script/UAX#14) + shape + fallback + portable `IGlyphAtlas`. *(Today: `Segoe UI` face, per-glyph CLEARTYPE raster → R8 atlas, LTR advances.)* |
| 19 | Text — layout engine, two-level cache, by-handle glyph emit | ⬜ | L1 shaped-run + L2 wrap cache; visual-order `GlyphRunHandle` emit; hit-test/selection rects. |
| 20 | SourceGen — Diff/Mount/HookDeps/SceneWriter + analyzers | ⬜ | Real Roslyn analyzers: ElementTypeId/DiffProps/Modifier/HookDeps/SceneWriter/Theme + `WGPU####` diagnostics. |
| 21 | Reconciler — full engine (keyed-LIS, component table, memo-skip, effects, destroy) | 🟡 | LIS-minimal moves, ComponentTable, UpdateQueue, 3-signal memo skip, EffectScheduler, ErrorBoundary. *(Today: keyed reconcile — identity-preserving reorder + removal; checks 17–18.)* |
| 22 | Render — SceneRecorder rewrite (dirty-walk, transforms, clean-span) | 🟡 🖼 | By-handle emit + `SortKey64` + WorldTransform + clean-span memcpy reuse + witness. *(Today: full-tree walk, translation+scale+rotate compositor, **clip-stack (intersected device rects) + per-node clip-culling of offscreen draws**.)* |
| 23 | Hosting — full 13-phase loop, coalescer, worker pool, app loop | 🟡 | `RenderPriorityPolicy` coalescer, `IPlatformAppLoop`, worker pool, per-phase alloc-tripwire. *(Today: pump→input→flush→render→reconcile→layout→6.5→record→submit→present→effects; dirty bool.)* |
| 24 | Hosting/Render/Rhi — render-thread split + retire-fence + quarantine flip | ⬜ 🖼 | Spawn render thread, migrate ComPtr ownership, retire-fence handshake, device-lost rendezvous. |
| 25 | Layout — incremental layout + absolute/overlay + RTL | ⬜ | Dirty-scoped relayout (O(0) idle), absolute positioning, `ResolveLogical` RTL, overlay placement. |
| 26 | Layout/Render — incremental pixel-snap + DPI | ⬜ 🖼 | `roundValueToPixelGrid` verbatim + committed-origin seeding + `WM_DPICHANGED`; golden at 1.25/1.5/1.75. |
| 27 | Render — clip stack + layers/offscreen + path tessellation + effects/blend | 🟡 🖼 | 3-tier clip (scissor/SDF/stencil), `LayerPool`, path tessellator, erf drop-shadow, blend presets. *(Today: **tier-1 axis-aligned scissor clip** — `PushClip`/`PopClip` opcodes + `ClipCmd`, recorder pushes the intersected device rect for `ClipsToBounds` nodes + culls offscreen draws; headless-verified (clip count/balance/cull). GPU `RSSetScissorRects` wiring + SDF/stencil rounded-clip + layers/effects remain — needs-pixels.)* |
| 28 | Layout — Grid (distinct true-tracks) | 🟡 | `GridDefinition` + `TrackDef` + 5-step true-tracks algorithm; golden vs CSS-Grid oracle. *(Today: **`GridEl`/`Ui.Grid`/`Ui.UniformGrid`** — `TrackSize` Pixel/Star/Auto column tracks + gaps + row-major auto-flow + per-row Auto/fixed height; width-aware measure (resolves inside a ScrollView); `GridSpec` side-table. Headless check 51 (star split + cell flow). Explicit cell placement/spanning + dense auto-flow + CSS-oracle golden remain.)* |
| 29 | Scene/Layout/Reconciler/Hooks — scroll-as-transform + list virtualization | 🟡 🖼 | ContentSize + `-ScrollOffset` transform; `VirtualList` + Fenwick `ExtentTable` + scroll-anchoring; `UseVirtual`. *(Today: **`ScrollState` column + scroll-as-transform** (Layout publishes ContentSize, Input owns `-ScrollOffset` written to the content child's `LocalTransform`, clamped, layout-free); **`ScrollEl`** (clip+scroll viewport); **`VirtualListEl`/`Virtual.List`/`Virtual.VariableList`** realize-window-as-keyed-children → existing keyed-LIS diff = recycle over the slab free-list (no second pool); **uniform O(1)** + **variable Fenwick `ExtentTable`** (O(log n) offset↔index, estimate-then-correct) + **scroll-anchoring**; wheel input + boundary-cross re-realize vs in-window transform-only frame. Headless checks 36–44: 10k rows→~window, recycle 30→30 over a fling, **0-alloc in-window scroll**, variable positions/anchoring. Remaining: `UseVirtual` hook ergonomics, grouping/sticky-headers, image-warm `UseVisibleRange`, render-thread quarantine, `VirtualGrid`.)* |
| 30 | Animation — engine + tracks + transitions + driven clocks (phase 7) | 🟡 | Easing/spring tracks → LocalTransform/Opacity (never LayoutDirty); shared-element; `UseSpring`. *(Today: `AnimEngine` eased Opacity/Translate timelines at phase 7, retarget, no-relayout; checks 22–23. Springs/scale-rotate/driven-clocks/hooks remain.)* |
| 31 | Hooks/Reconciler/Dsl/SourceGen — concurrent-era hooks + Suspense + lane lowering | ⬜ | `UseTransition`/`UseDeferredValue`/`UseDerived`/`UseOptimistic`, Suspense reveal FSM, lane/boundary generators. |
| 32 | Input — full dispatch, gestures, focus, commands, cursor, autoscroll | 🟡 | TransformChain hit-tester, tunnel/bubble, GestureArena, FocusEngine + Tab/XY, commands, cursor, edge-autoscroll. *(Today: reverse-z hit + click; **focus model + Tab/Shift+Tab + focus SCOPES (dialog trap) + XY arrows + gamepad translation**; **WinUI activation semantics: Enter-on-keydown, Space-arms-fires-on-keyup w/ Escape cancel**; **double/triple-click count → `OnPointerPressed`**; **right-tap + VK_APPS → `OnContextRequested`**; **keyboard accelerators (`BoxEl.Accelerator`) + access keys (`BoxEl.AccessKey`, Alt chord/tap mode)**; **WindowBlur cancel + blur hook**; **hover-resolved cursor → `IPlatformWindow.SetCursor`**; wheel → nearest-scrollable w/ edge-bubble; position-aware press/drag; checks 19–21 + E2.a–E2.l. Gesture arena / tunnel-Preview / momentum-inertia remain.)* |
| 33 | Parity + golden-image + zero-alloc gates (CI) | ⬜ 🖼 | Reactor-Yoga parity corpus, perceptual golden-image (CIEDE2000), path fuzz, BDN gen0/1/2==0, canon gates. |
| 34 | Media — image pipeline + video + lyrics | 🟡 🖼 | DecodeScheduler + ResidencyManager + `DrawImage`; video hole-punch present; synced-lyrics driven clock. *(Today: portable **`ImageCache`** (source-dedup, Pending→Ready state machine, **liveness ref-count pinning** — on-screen never evicted, LRU byte budget) + `IImageDecoder` seam (`FakeImageDecoder` headless; +decode pumped in phase 7.5, alloc-free) + **`ImageEl`/`Ui.Image`** + **`DrawImage` opcode** + recorder emit + reconciler request/pin-on-mount/unpin-on-recycle. Headless checks 45–46. Real WIC decode + GPU texture upload (D3D12 draws the placeholder tint meanwhile), video hole-punch, lyrics remain — needs-pixels.)* |
| 35 | Theme — tiered brushes, palette extraction, dynamic accent reactivity | 🟡 🖼 | 3-tier BrushHandle + `PaletteExtractor` + `ISystemColors` Epoch reactivity. *(Today: static `Theme` + real accent read + Mica.)* |
| 36 | Input.A11y/Text — UIA, IME, clipboard/drag-drop, overlays, selection, text document | 🟡 🖼 | UIA provider tree, Imm32 IME, selection/caret, editable `ITextDocument`, OverlayManager, OLE drag-drop. *(Today: **the full text-edit stack** — `TextEditCore` (gap-buffer doc, grapheme-safe, RichEdit word nav, coalescing undo), `IClipboard` + `IPlatformTextInput` PAL seams (Win32 CF_UNICODETEXT + Imm32 composition/clauses/candidate-window; headless fakes), `IFontSystem` hit-test/caret/range-rects, scene `TextEditState` + pooled decor slabs + WinUI-exact selection/caret/IME-underline emission, `CaretBlinker`, `EditableText` full editor + TextBox/PasswordBox/NumberBox/AutoSuggestBox/editable-ComboBox at WinUI parity; slice W0a/W0e/W0f, 195 checks. UIA TextPattern + OLE drag-drop remain.)* |
| 37 | Cross-cutting — backdrop/Mica, acrylic, localization, FastPath, AOT/footprint, hot-reload | 🟡 🖼 | `IBackdropSource` caps probe, acrylic effect runner, localization gen, AOT/footprint ratchet, hot-reload. *(Today: dark/Mica attributes, AOT-compatible props; **in-app acrylic effect runner LIVE on D3D12** (`FluentGpu.Windows/D3D12/AcrylicCompositor`) — per-`PushLayer` canvas-region snapshot → pooled pow2-bucket offscreen RTs (fence-gated deferred retire, zero steady-state texture creation) → fixed-kernel two-pass separable gaussian at a DPI-aware /down (effective σ = 30 DIP per AcrylicBrush.h `sc_blurRadius`; /4 @100%, /8 @200%) → WinUI luminosity/tint/2%-noise composite clipped to the rounded layer rect; recipe + region/bucket/kernel math headless-checked (slice 64m + 64n1–4 via portable `Render/AcrylicBackdropMath`); the composited GPU pixels themselves are needs-pixels (manual `--shot`).)* |

## Net-new this session (not in the original 37 — needed for the Wavee skeleton)
- **Core controls** (`FluentGpu.Engine/Dsl/Controls/Controls.cs`): `Slider` (press-to-set + drag-to-scrub), `ToggleButton`,
  `IconButton`, `ScrollBar` — stateless/controlled; headless checks 47–48.
- **Navigation** (`FluentGpu.Engine/Reconciler/Navigation.cs`): `Route`/`Navigator` (serializable back stack — push/pop/replace,
  `Serialize`/`Deserialize` for deep-link + cold-launch restore), `PageHost`, `Nav.Context`; headless checks 49–50.
- **Component-anchor pass-through fix** (`Reconciler.MirrorParticipation`): component + context-provider anchors now mirror
  their rendered child's flex participation, so a `Grow=1` page fills its content region (was collapsing to 0). This is the
  `layout.md §2.2` "passthrough child is layout-invisible" contract, realized.
- **Wavee skeleton acceptance test** (slice check 52; runnable `--demo wavee`): sidebar nav → `PageHost` back stack; Home
  (album-art card `Grid` in a `ScrollView`) + Playlist (5,000-row virtualized list, ~190 live nodes) + now-playing
  `PlayerBar` (Slider + transport `IconButton`s + `ToggleButton`). Composes every subsystem above at once.

## How this is being executed
Subsystem-by-subsystem, each **implemented → headless-verified (new slice/golden checks) → committed**. Headless-verifiable
steps are proven by deterministic structural/golden assertions + the zero-alloc tripwire. **needs-pixels** steps require
your on-screen confirmation (and a real GPU / assistive tech), since this environment renders without a visible display.
