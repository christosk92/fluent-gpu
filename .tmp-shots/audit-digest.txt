
=============== wake (5 findings) ===============
[wake-01|critical|certain|cpu] FG_DIAG silently mounts the dynamic-text HUD, which pins the full pipeline at vsync rate forever
    file: src/FluentGpu.WindowsApp/Gallery.cs:17
    cost: full record+submit+present pipeline at 60Hz forever while the HUD is mounted; ~5 string interns + 5 ToString allocations + AddRef/Release per frame
    fix: Decouple the HUD from FG_DIAG (HUD should be its own opt-in: keep FG_HUD, drop FG_DIAG from the ShowDiagnosticsHud expression at Gallery.cs:17), AND make the HUD not pin the full pipeline at vsync. Two complementary options: (a) clamp HUD refresh to a few Hz: instead of HasDynamicText forcing WaitForWork(0), have the host treat 'only HasDynamicText is active' as a timed wait (e.g. WaitForWork(150ms)) so the FPS/ms readout updates ~6Hz at ~0% idle CPU — tradeoff: the FPS/ms numbers update at 6Hz not per-frame, which is fine for a readout (and arguably more readable); (b) only mark dynamic-text dirty/repaint when a value actually changes (UpdateDynamicText at SceneStore.cs:593-605 already no-ops equal ids, but the FRAME still runs because HasDynamicText alone keeps HasActiveWork true). Best: add a separate HudThrottle term so 'HUD-only' active state paces at a slow timed wait rather than 0. Touch AppHost.cs:188-192 + FluentApp.cs:70 (pass a wait budget) + Gallery.cs:17.
[wake-02|high|certain|cpu] Indeterminate ProgressRing / loop:true keyframes keep AnimEngine.HasActive true forever (no occlusion/visibility gate)
    file: src/FluentGpu.Animation/AnimEngine.cs:166
    cost: full pipeline at 60Hz for as long as a spinner/loop-animation is mounted, even when not visible
    fix: This is intended for a VISIBLE spinner; the defect is the lack of a visibility/occlusion gate. Add window-visibility state (Win32Platform already detects SIZE_MINIMIZED at Win32Platform.cs:427 — surface it as IPlatformWindow.IsVisible/IsMinimized) and, when minimized or fully occluded, suppress the animation tick + skip record/submit/Present (or hold the loop on WaitForWork(-1) until a restore/paint message). Tradeoff: time-driven loop animations must catch up on restore (clamp the accumulated dt or just resync). For the gallery specifically, also consider that leaving the Animation/ProgressRing page mounted is inherently non-idle — acceptable, but the minimize case is pure waste. Touch FluentApp.cs:61-71 (gate the loop body on visibility), AppHost.cs (expose/consume an IsVisible check), Win32Platform.cs WM_SIZE/WM_ACTIVATEAPP.
[wake-03|high|likely|cpu] No minimize/occlusion gating: the whole record+submit+present pipeline runs into an invisible surface while minimized
    file: src/FluentGpu.WindowsApp/FluentApp.cs:61
    cost: full pipeline at ~60Hz (possibly faster if the composited waitable free-runs when minimized) for the entire time the window is minimized AND any wake term is active
    fix: Add an explicit visibility gate so frames stop when minimized/occluded. On WM_SIZE/SIZE_MINIMIZED set IsVisible=false; on restore set true and request one frame. In the loop, when !IsVisible, block on WaitForWork(-1) regardless of HasActiveWork (animations hold; reconcile work still queues and is flushed on restore). Optionally also honor DXGI_STATUS_OCCLUDED from Present to enter the same paused state until the next WM_PAINT. Tradeoff: a minimized window stops advancing time-driven animations (must resync dt on restore) and stops any FrameClock-driven background timers (acceptable — WinUI apps pause rendering when minimized). Touch FluentApp.cs:61-71, Win32Platform.cs WM_SIZE, AppHost.cs (expose a frame-suppress path).
[wake-04|medium|certain|cpu] ImageCache.PendingCount and HasActiveCrossfades are O(entries) scans evaluated on every HasActiveWork call
    file: src/FluentGpu.Scene/ImageCache.cs:139
    cost: 2x O(cached-image-count) scans per frame while the loop is awake; negligible on small pages, measurable with hundreds/thousands of entries (the wavee 10k-art workload)
    fix: Maintain running counters instead of scanning: increment/decrement a _pendingCount in OnDecodeComplete/Request (ImageCache.cs:159/240) and expose PendingCount as a field read. For crossfades, track the count of entries whose TextureMs is within DurationMs via a small active-set or an earliest-deadline timestamp (HasActiveCrossfades == _clockMs < _earliestCrossfadeEndMs). Touch ImageCache.cs:139 + 212-220, OnDecodeComplete, Request, Tick.
[wake-05|medium|likely|cpu] Paint() always runs record+submit+present with no compositor-only fast path; a transform-only animation frame pays full record
    file: src/FluentGpu.Hosting/AppHost.cs:492
    cost: full-scene SceneRecorder.Record + DrawList rebuild + GPU submit every vsync while any animation/HUD keeps the loop awake, regardless of how little actually changed
    fix: Add a 'dirty-region / paint-only' fast path: when no reconcile and no layout happened and only TransformDirty/PaintDirty bits are set on a bounded node set, either (a) re-record only from the nearest stable ancestor, or (b) keep a retained DrawList and patch the changed nodes' commands in place (the design's stated SoA + clean-span-reuse model supports this). Verify against the architecture-spec compositor-only-frame contract before implementing. Tradeoff: added complexity in the recorder + a correctness gate (clean-span reuse validity: handle IsLive && content-epoch unchanged && baked-geometry hash unchanged). Marked 'likely' because a partial-record path may already be partly present in SceneRecorder that I did not fully read. Touch AppHost.cs:485-498, SceneRecorder.
  wishlist:
    - awake-reason histogram: each frame, record WHICH HasActiveWork term(s) returned true (a bitmask over the 13 terms) and expose per-term 'frames-kept-awake' counters — instantly tells you it's HasDynamicText vs AnimEngine.HasActive vs an image scan pinning the loop
    - consecutive-awake-frame counter + wall-clock 'time since last idle (WaitForWork(-1)) block' — a healthy idle app should show the loop blocking within ~1s of last input; a value that never resets flags a wake-pin
    - AnimEngine: expose track count split by loop vs finite, and the node handles owning loop tracks, so a 'never-settles because loop:true' pin is attributable to the exact control (ProgressRing/Animation page)
    - HUD/dynamic-text gauge: _dynamicTextCount and a 'HUD is forcing full pipeline' boolean surfaced in the HUD itself (self-aware) so it is obvious the readout is the cause
    - window-visibility/occlusion state (IsMinimized/IsOccluded) + a counter of frames recorded-and-presented while not visible — should be 0 for a correct engine
    - per-frame 'work classification': did this frame reconcile? layout? record-full? compositor-only? present? — a stream of these makes 'full record every vsync for a 1px spinner' visible (relates to wake-05)
    - ImageCache: PendingCount/crossfade-active as O(1) counters surfaced in diag, plus 'HasActiveWork image-scan cost (ns)' so the O(N) scan regression (wake-04) is measurable
    - Present pacing probe: log the delta between successive Present() calls and the latency-waitable wait duration; a minimized window still presenting at ~16ms vs free-running <1ms distinguishes wake-03's bounded-vs-unbounded ambiguity on real hardware

=============== alloc (5 findings) ===============
[alloc-01|high|certain|cpu+alloc] _frameClockSig boxes a long every rendered frame AND forces a full re-render of every FrameClock subscriber
    file: src/FluentGpu.Hosting/AppHost.cs:517 (write); :105/:338 (Signal<object?> ambient); src/FluentGpu.Hooks/RenderContext.cs:208-209 (read); src/FluentGpu.Foundation/Signals/Signal.cs:33-37 (object equality)
    cost: ~24 B/frame boxing + one full component re-render/frame PER mounted FrameClock subscriber, at vsync (~60-165 Hz) for as long as it is mounted; e.g. a visible standalone ScrollBar or an open ToolTip pins it
    fix: Make the per-frame tick channel non-boxing: give the host a dedicated `Signal<long> _frameClockScalar` (or a FloatSignal-style LongSignal) and resolve FrameClock.Tick through a long-typed ambient rather than Signal<object?>. If the ambient-context plumbing must stay object-typed, at minimum stop re-rendering subscribers needlessly: the value monotonically increases, so the box+notify is intended, but the COST is the forced re-render — consider exposing FrameClock as an opt-in 'tick token' the subscriber reads via Peek + an explicit per-frame scheduled effect, or have controls subscribe only while their phase is live (ScrollBar/ToolTip already gate mount, but ScrollBar.cs subscribes unconditionally while mounted). Files: AppHost.cs (channel type + :517), RenderContext.cs UseContext path, Context.cs FrameClock. Tradeoff: a typed scalar context needs a parallel non-object ambient table in the reconciler (_ambient is Dictionary<object,Signal<object?>>).
[alloc-02|medium|certain|alloc] UpdateDynamicDiagnosticsText interns ~5 freshly-allocated ToString strings per frame (HUD path)
    file: src/FluentGpu.Hosting/AppHost.cs:817-829 (UpdateDynamicDiagnosticsText); src/FluentGpu.Scene/SceneStore.cs:593-605 (UpdateDynamicText); src/FluentGpu.Foundation/StringTable.cs:62-65 (Intern dedup needs an allocated string first)
    cost: ~5 strings/frame (~150-250 B/frame) at vsync, ONLY when FG_HUD or FG_DIAG is set (Gallery.cs:17 gates the HUD mount); combined with alloc-03 this is steady Gen0 churn forever
    fix: Format into a reused buffer and intern from a ReadOnlySpan<char>: add a StringTable.Intern(ReadOnlySpan<char>) overload that probes the map with a span comparer (or a small thread-local char[] + AlternateLookup<ReadOnlySpan<char>> on _map, .NET 9+) so the steady 'value unchanged' case never allocates. Also short-circuit per-kind: cache the last (kind→value) numeric and skip ToString when the numeric is unchanged (fps/cmd/draw/cull/ms rarely all change). Files: AppHost.cs:817-829, StringTable.cs (new span overload). Tradeoff: the span-keyed map lookup needs the alternate-lookup API or a custom probe.
[alloc-03|high|certain|cpu] Dynamic-text HUD pins HasActiveWork awake forever, re-running the full record+submit+present pipeline at vsync
    file: src/FluentGpu.Hosting/AppHost.cs:188 (HasActiveWork includes _scene.HasDynamicText); src/FluentGpu.Scene/SceneStore.cs:581 (_dynamicTextCount>0); src/FluentGpu.WindowsApp/FluentApp.cs:70 (WaitForWork(HasActiveWork?0:-1)); Gallery.cs:17,291,303
    cost: Full record(whole tree)+submit+present every vsync frame forever while the HUD is mounted (and the user reports the HUD visible, implying FG_DIAG/FG_HUD is set) — the dominant idle CPU driver in that mode; ~the entire phase-6..13 cost at 60-165 Hz
    fix: Decouple 'has dynamic text' from 'needs a frame': the HUD only needs to refresh when its source numbers (fps/ms/counts) actually change, which is at most once per real repaint. Remove _scene.HasDynamicText from HasActiveWork and instead have the host WakeFrame() only when a metric value changes (compare last-published fps/ms/counts; if equal, don't request a frame). Or throttle the HUD to a 250ms-500ms timer (UseContext(FrameClock) is the wrong tool — see alloc-01). Files: AppHost.cs:188 (drop the term), AppHost.cs:817-829 (publish-on-change + WakeFrame on change). Tradeoff: the FPS readout updates a few times/sec instead of every frame (visually identical) and the app idles correctly. Note this overlaps the wake-auditor's area; flagged here because it is the carrier for alloc-02.
[alloc-04|low|certain|alloc] FG_ALLOC_DIAG report builds a StringBuilder + interpolations every second (diag-only, but allocates on the UI thread)
    file: src/FluentGpu.Hosting/AppHost.cs:151-160 (DiagMaybeReport)
    cost: ~1 KB once per second, ONLY under FG_ALLOC_DIAG; self-skewing of the diag output, negligible otherwise
    fix: Reuse a single cached StringBuilder field (Clear() each report) and write segments with sb.Append(name).Append(' ').Append(kb).Append… avoiding the interpolated-string handler temporaries, or format into a pooled char[] and Console.Error.Write(span). Files: AppHost.cs:151-160. Tradeoff: none meaningful; purely tidies the measurement path.
[alloc-05|medium|likely|alloc] Per-segment alloc diag has no probe for the dynamic-text/ToString work or the FrameClock box+re-render — they hide in 'untracked'
    file: src/FluentGpu.Hosting/AppHost.cs:114-116 (segment set), :485 UpdateDynamicDiagnosticsText (between SegImages and SegRecord, unprobed), :516-517 PublishFrameStats + _frameClockSig write (after SegSubmit, inside the untracked tail), :502 DrainPassiveEffects (folded into SegEffects with the re-render it triggers)
    cost: n/a (diagnostic coverage gap) — but it is why these allocations were not already pinned
    fix: Add explicit segments: SegDynText (wrap :485 in its own Probe) and SegPublish (wrap :506-517). Attribute the FrameClock box to SegPublish and the resulting re-render is then visible as a SegFlush spike correlated with HasSubscribers. Files: AppHost.cs:114-116 (extend the segment enum + names), and add Probe calls around :485 and :516-517. Tradeoff: two more segments in the once-a-second line.
  wishlist:
    - An EventListener on Microsoft-Windows-DotNETRuntime GCAllocationTick_V4 (1 event ~ every 100KB allocated) that aggregates AllocationAmount by TypeName, sampled only across steady awake frames — this would immediately surface System.String (alloc-02 ToString), System.Object/boxed System.Int64 (alloc-01 _frameClockSig box), and System.Text.StringBuilder/char[] (alloc-04). Bucket by type and print top-10 bytes/sec.
    - A per-channel boxing counter on the ambient Signal<object?> publish path: increment a counter each time SetAmbient-backed signals (_frameClockSig/_frameStatsSig/_viewportSig) take the boxing assignment, exposed via Diag.Set('signals','ambientBoxes/s', n). A non-zero _frameClockSig count at idle is the alloc-01 smoking gun.
    - A 'forced re-render cause' counter: in ReactiveRuntime.Schedule or TreeReconciler.RunComponent, tag each scheduled render with the source signal and aggregate renders/sec by source. A steady stream attributed to the FrameClock ambient proves the re-render storm (alloc-01) independent of the boxing.
    - A StringTable.Intern hit/miss + allocations counter: Diag.Count('strings','internMiss') vs 'internHit', plus a 'ToString-then-intern' marker. A high internHit/s at idle (the same FPS string re-interned every frame) flags alloc-02 without a profiler.
    - Wire the FG_ALLOC_DIAG segment set to add SegDynText and SegPublish (see alloc-05) so UpdateDynamicDiagnosticsText and the PublishFrameStats/_frameClockSig tail stop hiding inside 'record' and 'untracked'.
    - A HasActiveWork breakdown probe: once/sec, print which OR-term in AppHost.cs:188-192 is keeping the loop awake (a bitmask: HasDynamicText|HasPending|anim|interact|scroll|repeat|caret|brush|images|orphans|drag). This is the single most useful idle-CPU diagnostic — it would have shown HasDynamicText pinned under FG_DIAG vs whatever pins a default run.

=============== leak (6 findings) ===============
[mem-01|high|likely|leak] DecodeScheduler._canceled grows unbounded: canceled image ids are never reclaimed unless re-requested
    file: src/FluentGpu.Media/DecodeScheduler.cs:32,66,132,145
    cost: ~1 entry (≈40-60 B incl. CHM node) per canceled-and-not-re-requested decode; on heavy virtualized scroll this is thousands/min of steady churn that never frees
    fix: Remove the id from _canceled at the end of Process() (it has terminal authority over that request) and also when a Done is drained in Pump(). Simplest: in Process()'s finally, `_canceled.TryRemove(req.Id, out _)`; and on the canceled-early returns. Tradeoff: a cancel that races just before claim still works (claim re-checks ContainsKey before the remove); the remove only fires once the worker owns/finishes the id. Alternatively cap _canceled and treat it as a bloom-ish recent-set. Add a `CanceledPending => _canceled.Count` accessor for the census.
[mem-02|high|certain|alloc] Grow-only structures ratchet to peak working set and never shrink (SceneStore SoA columns, pools, heaps, StringTable id space)
    file: src/FluentGpu.Scene/SceneStore.cs:812-826 (Grow); src/FluentGpu.Rhi.D3D12/GlyphRenderer.cs:103 (_quadPool); src/FluentGpu.Rhi.D3D12/ImageTextureStore.cs:75 (_pool)
    cost: O(peak nodes) × (~30 columns, many 8-32 B/elem) held forever; plus pooled GPU bucket textures (256²·4=256KB / 512²·4=1MB each) retained for the session; e.g. a session that ever showed 5k images can pin tens of MB of pool textures idle
    fix: This is a deliberate near-zero-alloc tradeoff (pools avoid re-alloc churn), so the fix is a BUDGET/trim pass, not removing the pools: (a) add a high-water-watermark trim to SceneStore (when LiveCount stays << _high for N seconds, compact the freelist and Array.Resize down — costs a one-time copy); (b) cap GlyphRenderer._quadPool and ImageTextureStore._pool depth per bucket and release the surplus behind the fence; (c) expose these high-waters to the census so the ratchet is visible. Tradeoff: trimming re-introduces some alloc on the next growth spike — gate it on idle.
[mem-03|medium|certain|cpu+alloc] HUD per-frame string interning burns StringTable ids forever and inflates Gen0 at vsync rate (when FG_HUD/FG_DIAG on)
    file: src/FluentGpu.Hosting/AppHost.cs:817-829; src/FluentGpu.Scene/SceneStore.cs:593-605; src/FluentGpu.Foundation/StringTable.cs:62-80
    cost: ~60-75 burned ids/sec + ~60-75 short-lived strings/sec at 60Hz forever while the HUD shows; the burned-id space is ~6.5M ids/24h (outer-array cost ~13KB, negligible) but the steady Gen0 string churn keeps the heap from settling
    fix: Cache the last formatted string per DynamicTextKind and skip Intern when the rounded value is unchanged (compare the quantized double, not the string). Better: give the HUD numbers a small fixed pool of pre-interned digit-strings or render them via a glyph-run path that doesn't go through the interner. Also reconsider showing 0.1ms precision (it changes every frame by design). This finding is primarily CPU/GC-pressure, not retained growth — the StringTable itself does not leak unboundedly.
[mem-04|low|likely|cpu+leak] A wedged exit-orphan animation track pins the orphan (and a vsync-rate wakeup) until MaxOrphans evicts it
    file: src/FluentGpu.Hosting/AppHost.cs:781-788; src/FluentGpu.Scene/SceneStore.cs:305-323; src/FluentGpu.Animation/AnimEngine.cs:444-448
    cost: ≤64 retained exit subtrees + their tracks, plus a permanent vsync-rate wake if even one track never reports Done; bounded but the CPU cost (never-idle loop) is the real damage
    fix: Add a max-lifetime backstop to orphans: stamp an enqueue frame/time in OrphanEntry and force-ReclaimOrphan after e.g. 2s regardless of HasTracks (mirrors the MaxOrphans safety valve but time-based). Also assert/Diag when an exit track exceeds its expected duration. Expose OrphanCount + oldest-orphan-age to the census.
[mem-05|low|speculative|leak] InputHooks.Current static singleton retains the last AppHost's delegates (and thus the host graph) for the process lifetime
    file: src/FluentGpu.Hosting/AppHost.cs:304; src/FluentGpu.Hooks/Context.cs:173
    cost: one whole prior AppHost object graph (scene+device+app) retained per host re-creation; zero for a single-host app
    fix: Null out (or restore) InputHooks.Current.Default's host-wired delegates in AppHost.Dispose(); or make the channel default per-host rather than a process static. Verify whether any production path recreates AppHost before prioritizing.
[mem-06|low|certain|leak] AnimEngine._transitions and SceneStore side-tables keyed by node INDEX rely on slot-reuse self-cleaning, not on FreeSubtree
    file: src/FluentGpu.Animation/AnimEngine.cs:160-162,294-298; src/FluentGpu.Scene/SceneStore.cs:204-270 (FreeSubtree)
    cost: one LayoutTransition struct per index ever used for an Animate, retained to _high high-water; bounded, never trimmed
    fix: Either have the reconciler's Remove/UnmountSubtree call Anim.ClearTransition(node) on free (symmetric teardown, eliminates the dormant-stale window), or accept the bounded-by-slab contract and surface _transitions.Count in the census so the high-water is observable. Low priority — it's bounded by design.
  wishlist:
    - MemCensus.SceneNodesHigh: read SceneStore._high (add `public int Capacity => _gen.Length;` + existing LiveCount). Invariant: LiveCount returns near baseline after navigating away from a heavy page; Capacity is the never-shrinking high-water (flag if Capacity >> LiveCount for >N sec = trim candidate).
    - MemCensus.StringTable: sample MapCount (live interned), PendingReclaim (_pendingClear.Count), and ADD `public int IdHighWater => _count;`. Invariant: MapCount + PendingReclaim return to baseline after unmount; IdHighWater is monotonic — a steadily-climbing IdHighWater at idle pinpoints the per-frame HUD intern (mem-03).
    - MemCensus.ImageCache: sample existing Count, ReadyCount, PendingCount, UsedBytes, and the private _totalEvicted (add accessor). Invariant: UsedBytes ≤ budget always; Count returns to ~baseline after scrolling a list off-screen (pinned==on-screen only). A Count that climbs with RefsOf==0 entries that never evict = pin/unpin imbalance.
    - MemCensus.DecodeScheduler: ADD `public int CanceledPending => _canceled.Count;` plus existing Inflight/queued. Invariant: CanceledPending must trend to ~0 between scroll bursts; a monotonic climb is mem-01 directly.
    - MemCensus.ImageTextureStore (GPU): sample AtlasImages, PoolImages, DroppedThisRun, and ADD pooled-texture counts per bucket (`_pool[b].Count`) + _retired.Count. Invariant: pooled-texture totals are bounded by the working set; _retired drains within 2 frames (a growing _retired = the fence-release path stalled).
    - MemCensus.GlyphRenderer: sample CachedGlyphs, CachedRuns, and ADD a _quadPool total-retained-arrays accessor + _atlasEpoch (generation-reset count). Invariant: CachedRuns tracks the live text working set (drops after EvictStaleRuns); a climbing _atlasEpoch at idle means atlas thrash; a huge _quadPool retained-count after a scroll storm is the mem-02 ratchet.
    - MemCensus.Orphans/Anim: sample SceneStore.OrphanCount, AnimEngine._tracks.Count, _transitions.Count (add accessors), and the OLDEST orphan age (add a timestamp to OrphanEntry). Invariant: OrphanCount and _tracks.Count return to 0 shortly after any transition; a non-zero OrphanCount with stable age = a wedged exit track (mem-04).
    - MemCensus.Overlays: sample OverlayServiceImpl.Entries.Count and AppHost._popupWindows.Count. Invariant: both return to 0 after all flyouts close; a residual Entry or PopupWindowSlot = a Finalize/ClosePopupWindow that didn't run.
    - MemCensus.Reconciler: ADD accessors for _comps.Count, _live.Count, _virtuals.Count, _nodeBindings.Count, _providerSig.Count. Invariant: every one returns to the static-tree baseline after unmounting a page; a climbing _nodeBindings or _comps across navigations = an UnmountSubtree miss.
    - MemCensus.ManagedHeap: sample GC.GetTotalMemory(false) and GC.GetGCMemoryInfo().HeapSizeBytes once/sec at idle. Invariant: at true idle (no HUD) the heap settles flat within a few seconds; a sawtooth that never flattens at idle corroborates the HUD per-frame churn (mem-03) or another per-frame allocator.

=============== gpu (6 findings) ===============
[present-01|critical|certain|cpu] HUD DynamicText pins HasActiveWork true → the whole record+submit+present pipeline runs at vsync rate forever when idle
    file: src/FluentGpu.WindowsApp/Gallery.cs:303 (+439); src/FluentGpu.Hosting/AppHost.cs:188; src/FluentGpu.WindowsApp/FluentApp.cs:70
    cost: Full reconcile-flush + record + GPU record/submit/present pipeline at the monitor refresh rate (e.g. 60-165 Hz) with zero user activity, indefinitely. This is the primary driver of the reported persistent idle CPU.
    fix: Drop FrameMs/FrameFps (the two genuinely per-frame metrics) from the steady idle gate. Options, least-invasive first: (a) gate the HUD so it only forces wakes when actually changing — e.g. only count a DynamicText node toward HasActiveWork if its resolved value changed, or throttle FPS/ms HUD updates to ~2-4 Hz via a timer rather than every frame; (b) split HasActiveWork into 'needs a frame now' vs 'has a low-rate poller' and, when only low-rate pollers are active, pass a real timeout (e.g. 250ms) to WaitForWork so the loop sleeps between HUD refreshes instead of spinning at vsync; (c) make the gallery not mount the HUD by default (debug-only / hotkey). Tradeoff: the FPS/ms readouts update at the throttled cadence instead of every frame — acceptable for a diagnostics HUD. Note this is the render-side surfacing of the same wake-pin the orchestrator already suspects; the GPU pipeline is the victim, not the cause.
[present-02|high|likely|cpu] No minimized/occluded gate: a minimized or fully-covered gallery runs the full GPU pipeline every frame; DXGI_STATUS_OCCLUDED is ignored
    file: src/FluentGpu.Rhi.D3D12/D3D12Device.cs:914-920; src/FluentGpu.Pal.Windows/Win32Platform.cs:424-427; src/FluentGpu.WindowsApp/FluentApp.cs:61-71
    cost: Full per-frame UI-thread record + GPU submit while minimized or fully occluded, indefinitely (combined with present-01's wake-pin). Either a refresh-rate full pipeline (if Present still paces) or an unbounded busy-spin (if Present stops pacing when occluded).
    fix: Two parts. (1) Inspect the Present HRESULT: store `_occluded = (hr == DXGI_STATUS_OCCLUDED)` and on the next frame call `Present(0, DXGI_PRESENT_TEST)` to detect when the window becomes visible again, skipping record+submit while occluded — the standard DXGI occlusion pattern. (2) Gate the loop on window state: when `_window.State == WindowState.Minimized` (Win32Platform.cs:292), skip RunFrame's paint entirely and block in WaitForWork(-1) until a real message arrives (a restore/paint). Tradeoff: a tiny amount of PAL→host plumbing (expose minimized to the host loop) and the occlusion test costs one cheap Present(TEST) per occluded frame instead of a full pipeline.
[submit-01|medium|certain|cpu] Whole DrawList is re-decoded and every instance re-uploaded each frame — no dirty-skip / no retained command buffer
    file: src/FluentGpu.Rhi.D3D12/D3D12Device.cs:285-357,738-769; RoundRectPipeline.cs:359-376; GlyphRenderer.cs:809-828 (and Shadow/Arc/Polyline/Gradient Record)
    cost: For a static ~1.5k-command scene: one full-stream scan + ~1.5k POD reads + struct builds + list appends + glyph-run cache lookups, plus a memcpy of all instances (order of ~150-300 KB of instance bytes for a text-heavy gallery page) into upload buffers, every frame. ~hundreds of microseconds of UI-thread CPU per frame; harmless once, wasteful at vsync-forever.
    fix: Primary fix is present-01/present-02 (stop running frames when nothing changed) — that removes ~all of this cost without touching the recorder. If a steady high-FPS scene (video/animation) later needs it, the avoidable pieces in priority order: (a) skip the separate `StreamHasLayer` pre-scan by having the recorder set a hasLayers flag on the DrawList; (b) cache the decoded instance lists + run list keyed on a DrawList content hash and skip Decode when the stream is byte-identical to last frame (the recorder already produces deterministic bytes); (c) for fully-static frames, skip re-recording the command list entirely and just re-Present the last back buffer (requires tracking that the scene/clear are unchanged). Tradeoff: (b)/(c) add a dirty-tracking layer and a content hash to an otherwise stateless recorder — only worth it if frames must run continuously.
[submit-02|low|certain|cpu] Glyph atlas is the only meaningful per-frame texture upload and it is correctly dirty-gated; flag the failure mode, not steady state
    file: src/FluentGpu.Rhi.D3D12/GlyphRenderer.cs:679-704,797-807,352-378,526-566
    cost: Steady state: zero atlas uploads, zero re-rasterization. Pathological: a continuous font-size/scale animation re-rasterizes every glyph every frame (CreateGlyphRunAnalysis + CreateAlphaTexture + grayscale conversion + 4 MB upload), and an oversized catalog (Iconography page) can thrash ResetAtlas.
    fix: No steady-state change needed — this path is well-built. Guard the failure modes: (a) expose `RunsShaped`/`generation-reset` as standing diagnostics (already in Diag text.run/text.atlas at D3D12Device.cs:349 and GlyphRenderer.cs:376) so atlas thrash is visible; (b) consider a second atlas page or LRU eviction instead of a full generational reset when the 2048² fills (the Iconography catalog is explicitly called out at GlyphRenderer.cs:65-68 as near the limit). Tradeoff: multi-page atlas adds descriptor-table complexity; only needed if the reset count climbs in practice.
[mem-01|low|certain|leak] D3D12MemoryDiagnostics is blind to committed-resource totals, atlas/upload bytes as a class, descriptor heaps, and the screenshot readback buffer
    file: src/FluentGpu.Rhi.D3D12/D3D12MemoryDiagnostics.cs:20-72; D3D12Device.cs:970-1031
    cost: Diagnostics blind spot: committed VRAM total and atlas/upload byte class are invisible at runtime, so a residency or upload-buffer growth issue wouldn't show in the standing HUD. CaptureBgra is a debug-only path (screenshot tooling) — not a steady leak, but each call stalls the GPU and allocates a full-frame managed array + a committed readback resource.
    fix: (1) Surface the census as live counters: `Diag.Set("d3d12.mem", "liveBytes"/"resourceCount", …)` from a per-frame or per-N-frame tick, and add an Initialize-time call to D3D12MemoryDiagnostics from the diagnostics module so it is on without FG_D3D_MEM. (2) Track descriptor heaps and the CaptureBgra readback buffer through the same Track/Release. (3) If screenshots are ever taken in a loop, pool the readback buffer + reuse the managed array instead of per-call create/destroy. Tradeoff: a few extra Track calls and one periodic Diag.Set; negligible.
[present-03|low|certain|cpu] Vsync is on by default and nothing in the gallery flips it; interval=0/ALLOW_TEARING is unreachable on the composited path
    file: src/FluentGpu.Rhi.D3D12/D3D12Device.cs:44,229,912-920
    cost: None as a defect — confirms the active loop is refresh-rate-bounded (not free-running) in the gallery. The cost remains that it runs that bounded pipeline forever (present-01), not that it exceeds refresh.
    fix: No change required. If an uncapped-FPS diagnostic mode is ever added, note that on the composited swapchain interval-0 won't tear (ALLOW_TEARING is hwnd-only); the wait that actually paces is WaitForLatency (D3D12Device.cs:287,939-942), called at the TOP of SubmitDrawList (before record), so frame N's latency wait overlaps frame N-1's GPU work — correct double-buffered pacing with FRAME_COUNT=2.
  wishlist:
    - frames-per-second actually submitted while idle (no input, no animation) — should be ~0; a steady refresh-rate count is the wake-pin smoking gun
    - a 'why awake' breakdown: expose each HasActiveWork sub-predicate (HasDynamicText, HasPending, anim.HasActive, …) as a named counter so the pin source is one glance, not a guess
    - window occlusion/visibility state (IsIconic / DXGI_STATUS_OCCLUDED hit count) and frames-submitted-while-occluded — catches the minimized-burns-CPU class
    - Present() HRESULT histogram (S_OK vs DXGI_STATUS_OCCLUDED vs error) — currently the occluded status is silently swallowed by Check()
    - per-frame submit CPU: ticks for StreamHasLayer + Decode + RecordAll separately (the FG_ALLOC_DIAG SegRecord/SegSubmit split already exists for ticks; surface it as a live counter, not just the 1Hz stderr dump)
    - instance bytes memcpy'd into upload buffers per frame (sum of all pipeline Record copies) — quantifies the re-upload-everything cost vs a dirty-skip
    - D3D12 committed-resource total bytes + count (the census D3D12MemoryDiagnostics keeps internally is never surfaced as a Diag counter; only logged under FG_D3D_MEM)
    - glyph atlas occupancy + generation-reset count + run-cache size/shaped-vs-cached (already in Diag text.atlas/text.run — wire into the standing diagnostics module so atlas thrash is visible)
    - CaptureBgra readback-buffer create/destroy count (per-call committed READBACK alloc + GPU stall) — flags screenshot-path leaks/stalls

